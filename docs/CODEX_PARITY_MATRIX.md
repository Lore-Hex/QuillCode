# Codex Parity Matrix

| Area | Feature | Status | Notes |
| --- | --- | --- | --- |
| Core | Threads and transcript persistence | Implemented | JSON thread store, desktop bootstrap loading, and mock agent runner. |
| Core | Project picker/sidebar | Partial | Workspace state model, persisted project registry, selected-project context, native project rail, visible New chat/Search/Open project/Terminal actions, desktop folder picker, current-directory desktop project seed, project row actions for New chat, Refresh context, Rename, and Remove from list, thread selection, visible rename/duplicate/pin/archive/unarchive/delete controls, bulk select/select-all/pin/unpin/archive/unarchive/delete actions, pinned/recent/archived chat grouping, archived-state search, command-palette/slash routing, and Playwright coverage exist. Richer sidebar drag/drop, saved filters, and full Codex workspace list polish pending. |
| Core | TrustedRouter runtime | Partial | CLI and desktop can use live TrustedRouter when an env or stored secret key exists; CLI auth key management and native developer settings exist; desktop has an explicit OAuth-first auth mode, loopback PKCE sign-in action, pure authorize/exchange/userinfo OAuth client, persisted delegated key storage, non-secret signed-in account display, and developer override as a secondary mode; desktop refreshes the live model catalog when keyed. Agent runs now expose incremental thread progress, consume live TrustedRouter action text as a stream, show Streaming status while chunks arrive, render safe `say` action text as a live assistant draft without leaking tool JSON, support bounded multi-step tool continuation with hidden tool-feedback messages, surface actionable runtime issue cards/pills for missing sign-in, missing developer key, rejected key, rate limit, network failure, empty response, malformed model action, and generic run failure, can retry the latest user turn through the same send path, opens the searchable model picker directly from malformed-response and rate-limit recovery, and shows redacted runtime diagnostics plus parsed rate-limit metadata in Settings. Account recovery telemetry and provider status diagnostics beyond local runtime state pending. |
| Core | TrustedRouter model picker | Partial | Catalog model, live fetch adapter, searchable grouped provider/category/model SwiftUI picker, persistent user favorites, recent-model surfacing from thread history, current/default/recommended/favorite badges, deterministic provider/category/model/default/recommended/favorite metadata rows, inline provider-detail browsing, duplicate-free normal search over row metadata, and Playwright coverage exist. Richer live provider capability metadata pending. |
| Tools | Shell commands | Implemented | `host.shell.run` with empty-command guard. |
| Tools | File read/write | Implemented | Workspace-scoped UTF-8 files. |
| Tools | Apply patch | Implemented | Workspace-scoped unified diff application through `git apply`. |
| Tools | Git status/diff/stage/restore/commit/push/PR/worktrees | Partial | Status/diff, file-level stage/restore, hunk-level stage/restore, local commit, conservative named-remote branch push, GitHub PR creation via structured `gh pr create`, and worktree list/create/remove are implemented with workspace path, patch, ref-name, and registered-worktree checks. Rich GitHub issue/PR review workflows pending. |
| Safety | Read-only/Review/Auto modes | Implemented | Auto can use static or model reviewer; SwiftUI shell can switch and persist modes. |
| Safety | Reviewer model call | Partial | TrustedRouter client, native OAuth/developer settings state, and desktop loopback OAuth wiring exist. Rich reviewer telemetry and production prompt tuning pending. |
| UX | Tool cards | Partial | Tool-card presentation model, HTML harness, and SwiftUI shell render queued/running/done/failed/review states; agent progress callbacks update the selected thread while a run is in flight; completed tool calls also produce human final-answer bubbles for common actions while full raw output stays behind card details; transcript rendering now interleaves messages and tool cards chronologically from the event timeline; hidden model-facing tool feedback is filtered from transcript surfaces, sidebar search, fork seeds, and compaction summaries; tool-result artifacts are surfaced as visible file/URL preview chips, local source/text artifacts render bounded UTF-8 previews, and image-like artifacts render bounded previews in SwiftUI, static HTML, and the Playwright harness; live TrustedRouter runs surface Streaming status before queued tools/final answers; safe streamed `say` text updates the assistant bubble before finalization; stopped queued/running tools close as failed `Stopped by user` cards; messages and tool cards expose lightweight Copy/Copy output actions with visible `Copied` feedback, user messages can be reused as focused composer drafts, assistant messages expose Helpful/Not helpful feedback backed by thread events, and the latest assistant response exposes a Retry action that reuses the normal retry command in SwiftUI, static HTML, and the Playwright harness. The Playwright harness now enforces first-pass interface polish primitives for smoothing, wrapping, tabular counters, hit areas, transitions, press feedback, radii, and image outlines. Broader native QuillUI polish and richer structured document/media previews pending. |
| UX | Activity/task pane | Partial | A right-side Activity pane is derived from the selected thread event log, tool cards, active instructions, memories, artifacts, latest assistant answer, deterministic task plan, and handoff summary. SwiftUI, static HTML, and the Playwright harness share the same surface contract, including command-driven collapsible Task Plan/Recent/Handoff/Tools/Sources/Artifacts/Latest Answer sections, and Playwright covers task/source/tool/artifact/plan/handoff rendering plus collapse behavior. Model-authored planning and richer task dependencies remain pending. |
| UX | Review pane | Partial | Latest completed `host.git.diff` is summarized into a changed-file pane in SwiftUI and the Playwright harness; successful `host.apply_patch` runs immediately refresh `host.git.diff` so edits flow into review; file-level and hunk-level Stage/Restore controls execute workspace-scoped git tools and refresh the diff; file-scoped notes plus line and range review comments are replayed from thread events and hidden when stale. Richer GitHub review workflows pending. |
| UX | Top bar widget | Partial | Top-bar presentation state, HTML harness, SwiftUI shell, and macOS `MenuBarExtra` track thread/project/model/mode/status/Computer Use and expose quick New Chat, Open Project, Command Palette, Terminal, Browser, Settings, Stop All, and disabled Disconnect All affordances. Stop All cancels active sends and integrated-terminal commands, and the composer mirrors that path with an active-run Stop button. Rich live progress, agent-router process cancellation, remote connection controls, and Linux tray adapter pending. |
| UX | Context and rate banners | Partial | Estimated context pressure banner appears near the local budget and offers Compact context, New thread, and Fork from last. Compact context creates a bounded continuation thread while preserving project/mode/model/instructions/memories. Exact provider token accounting, model-backed summaries, and richer fork context controls pending. |
| UX | Keyboard shortcuts/slash commands | Partial | Local slash command parser handles `/help`, `/status`, `/new`, `/rename title`, `/duplicate`, `/archive`, `/unarchive`, `/project new|refresh|rename|remove`, `/compact`, `/mode`, `/model`, `/terminal`, `/browser`, `/worktrees`, `/pr`, `/memories`, `/remember text`, and `/env [name]`; workspace-action slash commands share the same model/tool paths as visible UI and the command palette. The composer now shows filtered slash suggestions from the same catalog as `/help`, with keyboard selected-row navigation, Enter/Tab accept behavior, and click-to-insert command skeletons in SwiftUI and the Playwright harness. Search opens a finder over thread title, model, pinned/archived state, and indexed transcript/tool content. Active chat Find is implemented as a `Cmd+F` transcript bar with result counts, next/previous navigation, focused input, active-result highlighting, and command-palette access. A grouped command palette over workspace commands exists in SwiftUI and the Playwright harness, with registry-backed shortcuts, keyword/shortcut/title ranking, selected-row keyboard navigation, and E2E-tested primary workspace actions. `Cmd+/` opens a command-derived Keyboard Shortcuts panel in SwiftUI, desktop menus, menu bar, and the Playwright harness. Complete Codex shortcut parity pending. |
| Workspace | Integrated terminal | Partial | Workspace-scoped terminal pane, command history, live stdout/stderr streaming for running commands, persistent per-project working directory and exported/unset environment deltas, explicit running/done/failed/stopped command lifecycle, terminal-local Stop, Stop All integration, cancellable command execution, native SwiftUI surface, static HTML rendering, and Playwright coverage exist; full interactive PTY job control and curses-style TUI handling pending. |
| Workspace | Worktrees | Partial | Core `git worktree` list/create/remove tools, shared command-palette actions, create/remove dialogs, and create-time handoff into a selected worktree project/thread exist. Rich branch lifecycle polish, PR handoff, and cleanup affordances pending. |
| Workspace | Local environment actions | Partial | Project-local `.quillcode/actions/*.sh` and `.quillcode/local-env/*.sh` scripts are discovered with symlink/root bounds and exposed in the command palette plus `/env [name]` slash command as `Run ...` actions that dispatch through `host.shell.run`. Setup config metadata, scheduling, and richer local-env orchestration pending. |
| Workspace | AGENTS/rules instructions | Partial | Project-local and nested `AGENTS.md`, `.quillcode/rules.md`, and `.quillcode/instructions.md` are bounded, persisted on project refs, copied into thread context in broad-to-specific order, injected into TrustedRouter as hidden system context, and surfaced as a top-bar status. Per-file rule scopes and richer conflict display pending. |
| Browser | In-app browser | Partial | Workspace browser panel, address normalization for web/localhost/file/project-relative targets, bounded local HTML metadata snapshots, bounded HTTP(S) HTML snapshot fetching for reachable web/localhost pages, explicit inspection-depth labels, visible page outlines, text snippets, web/localhost source summaries, browser comments, SwiftUI surface, HTML renderer, structured `host.browser.inspect` agent tool, and Playwright coverage exist. Native WebView/rendering adapter, live DOM inspection for dynamic network-loaded pages, and signed-in browser profile support pending. |
| Computer Use | Backend protocol | Partial | Backend protocol, deterministic stub, macOS permission-aware status, screenshot capture, mouse, keyboard, and scroll adapter exist. The desktop window and menu bar render specific missing-permission labels, Settings includes an actionable Computer Use card with Screen Recording, Accessibility, setup, and refresh commands, and agent runs can dispatch `host.computer.screenshot`, click, type, scroll, move, and key tools through the active backend with concise final answers. Screenshots are stored as artifacts instead of raw base64 in transcript details, render as image previews on tool cards, and bounded multi-step turns can feed screenshot metadata back to the model for the next action. Linux backend, app approvals, richer permission education, and visual screenshot interpretation pending. |
| Plugins | Skills/plugins/MCP | Partial | Project-local `.quillcode/plugins/*.json`, `.quillcode/skills/*.json`, and `.quillcode/mcp/*.json` manifests are discovered with symlink/root and byte/count bounds, surfaced in the Extensions pane, command palette, static HTML renderer, macOS menu bar, and Playwright harness. MCP stdio manifests preserve structured command/args and can be explicitly started/stopped from the Extensions pane or command palette. Start performs a bounded MCP `initialize` plus `tools/list` probe before showing Ready, surfaces server/protocol/tool metadata, and Stop All terminates active MCP servers. Ready MCP servers expose a generic `host.mcp.call` agent tool that is allowlisted by the probed tool names and returns MCP text content into the normal tool-card/final-answer flow. Install/update lifecycle, richer per-tool schemas, MCP resources/prompts, and executable plugin wiring pending. |
| Long-running | Automations/subagents | Deferred | Planned after stable thread runtime. |
| Memory | Memories/Chronicle | Partial | Local memories load from `~/.quillcode/memories` and project `.quillcode/memories`, with extension allow-listing, symlink/root escape rejection, count/file/total byte caps, truncation labels, thread snapshots, TrustedRouter system-context injection, top-bar status, Memories pane, static HTML, Playwright coverage, slash-command visibility, command-palette Add memory prefill, `/remember text` explicit global writes, global Forget action, transcript notices, and basic credential/token/password/private-key rejection. Autonomous memory writes, richer redaction review, idle Chronicle jobs, conflict UI, and memory editing remain pending. |
| Remote | Phone/SSH remote | Deferred | QuillCloud/SSH design pending. |
| Artifacts | Tool-result artifact links and previews | Partial | Tool-result artifacts now render as visible file/URL preview chips with labels and source details in SwiftUI, static HTML, and the Playwright harness. Local source/text file artifacts render bounded UTF-8 previews with binary, byte, line, and extension guards. Image-like file, web, and inline data artifacts render bounded visual previews below the chips. Rich previews for PDFs, docs, sheets, slides, appshots, and generated media metadata pending. |
