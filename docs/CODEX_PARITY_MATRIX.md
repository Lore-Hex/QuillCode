# Codex Parity Matrix

| Area | Feature | Status | Notes |
| --- | --- | --- | --- |
| CLI | App-server WebSocket transports | Implemented | `quill-code app-server --listen ws://IP:PORT` and both `unix://` forms implement the current Codex WebSocket contract: standard HTTP Upgrade, one JSON-RPC message per text frame, masked-client enforcement, fragmentation, ping/pong/close handling, binary-frame dropping, bounded frame and queue sizes, exact `-32001` overload responses, slow-client disconnects, independent per-client sessions, and deterministic disconnect cleanup. TCP serves unauthenticated `/readyz` and `/healthz`, rejects every request carrying `Origin`, permits unauthenticated loopback only, and supports pre-initialize capability-token SHA-256 or signed HS256 bearer authentication with issuer/audience/time validation. Unix sockets remain private `0600` endpoints under a `0700` default directory, preserve active/non-socket paths, recover stale same-user sockets only after identity revalidation, and now use the same WebSocket connection handler as TCP instead of a private JSONL dialect. Focused protocol/auth/C/Swift tests plus executable TCP and multi-client crash/restart Unix smokes cover macOS and Linux. |
| CLI | App-server execution environments | Partial | Current Codex-compatible `environment/add` validates a nonempty `ws://` or `wss://` exec-server URL before registration, immediately registers or replaces a process-scoped exec-server WebSocket, and connects in the background; malformed, disabled, or non-WebSocket endpoints fail before a client or background task is created. `environment/info` performs or resumes the initialize/initialized handshake before returning remote shell and CWD metadata. Observation-only `environment/status` reports ready, pending, disconnected-with-error, or unknown-with-error without starting or recovering a connection, and probes ready remotes over the existing connection. A persistent request-ID-routed receive loop detects idle closure, preserves resumable sessions, and emits future-only `thread/environment/connected` or `thread/environment/disconnected` notifications to every subscribed thread selecting that environment without replaying current state. Selection changes, thread deletion, unsubscribe state, and session teardown suppress stale delivery. Thread start/resume/fork and turn start preserve omitted/default, empty/disabled, and first-selected environment semantics with sticky persisted selection. Selected remote turns replace local host routing with bounded remote shell, file read/list/search/write, and patch tools, inject escaped target-native environment context transiently, enforce canonical workspace containment and read-before-write, create stdin/patch temporary files under the remote workspace with target-native shell quoting, leave web search on its cloud path, and never fall back locally. Process and filesystem requests carry one immutable exec-server sandbox context derived from the thread policy and target workspace: read-only, workspace-write with writable-root/exclusion projection, and danger-full-access profiles serialize to the current protocol without null or host-local fallback; process starts also forward structured managed-network requirements with `enforceManagedNetwork` when a managed profile exists. File search is shell-independent: it canonicalizes the requested root, recursively scans through exec-server filesystem RPCs, skips dependency/build directories and oversized/non-UTF-8 files, and works for POSIX and Windows/PowerShell remotes without `rg`, `grep`, or local fallback. Process reads advance the inclusive `afterSeq` cursor without skipping events and wait for stream closure so late output is retained. Remote user-shell sessions stream stdout/stderr before exit and share the app-server background-process registry: they receive stable signed-32-bit app-server process IDs, expose null OS PIDs, and support acknowledged terminate/clean without resetting the multiplexed WebSocket when an obsolete long-poll reply arrives late. Direct `thread/shellCommand` uses the same selected target and one-hour timeout; disabled access rejects before lifecycle or dispatch. The registry is shared across clients of one app-server process and closes on transport shutdown. Focused actor/path/real-WebSocket tests plus a built-process loopback exec-server smoke cover status, no-replay lifecycle transitions, resumable disconnect/reconnect, dispatch, live and late output, background list/terminate/clean, exact sandbox forwarding, managed-network forwarding, target-native remote temporary path handling, shell-independent remote search, and local-sentinel non-execution. |
| CLI | App-server thread controls | Implemented | Codex 0.142.5-compatible `thread/unsubscribe` separates detailed-event subscription from connection-local loaded state and resume re-subscribes. `thread/increment_elicitation` and `thread/decrement_elicitation` maintain overflow-safe connection-local pause counts. `thread/metadata/update` durably patches nullable Git metadata. `thread/settings/update` persists model, effort, cwd, approval, sandbox or built-in permission profile, personality, service tier, summary, and collaboration mode, emitting `thread/settings/updated` only after its response and only on change. `thread/memoryMode/set` durably hides memory from model context without deleting stored notes. Focused actor tests, an executable JSONL smoke, research probes, and a parity gate cover response/error/null/ordering/reconnect behavior. Custom permission profiles and external-sandbox policy remain in the broader partial app-server row; external sandbox requests now return a dedicated compatibility-boundary error. |
| CLI | App-server persisted item pagination | Implemented | Current Codex-compatible experimental `thread/items/list` returns full persisted item payloads across one thread, optionally filters by `turnId`, defaults to ascending order and 25 items, clamps pages to 1...100, and returns stable forward/reverse opaque cursors. Cursor anchors are resolved in the complete thread item stream before filtering, so the same cursor remains valid when clients add or remove a turn filter, matching Codex's store contract. The method reads durable history without resuming the thread, merges QuillCode's active in-progress projection, preserves legacy `thread/turns/items/list` as an explicit unsupported method, and fails closed on malformed anchors or projected records without stable IDs. Focused actor tests cover full payloads, active turns, persistence order, filters, both directions, cursor reuse, bounds, empty history, and validation; the executable JSONL smoke and a source parity gate bind the contract. |
| CLI | App-server model-only context injection | Implemented | Codex 0.142.5-compatible `thread/inject_items` validates a nonempty array of raw Responses API items, accepts inline data images and forward-compatible roles, rejects remote images and unavailable tasks, returns `{}` without transcript lifecycle, and persists the items in a separate model-only timeline. Before-first and last-message anchors preserve injection order; active turn/review/compaction/user-shell snapshots cannot overwrite concurrent injections. TrustedRouter projection preserves message role/text/image semantics and renders non-message variants as canonical model context without fabricating executable tool calls. Focused protocol, persistence, prompt-order, cache-window, active-turn race, executable JSONL smoke, research, and parity-gate coverage prove the boundary. |
| CLI | App-server direct user shell | Implemented | Codex 0.142.5-compatible `thread/shellCommand` trims and validates one command, responds before lifecycle output, executes through the thread's selected local or remote login shell with a one-hour timeout, emits a `userShell` command item, and returns bounded output to model context. Empty environment access rejects before lifecycle or dispatch rather than falling back to the app-server host. Standalone commands share one concurrent turn, persist as empty history turns without command items, survive restart/fork/list/read, and roll back with their hidden output. Commands issued during ordinary turns, review, or compaction reuse that active turn; parent completion waits for attached shell work, while interruption and EOF cancel and drain it. Local and remote execution stream incremental stdout/stderr; remote execution uses the selected resumable exec-server process, participates in background-terminal lifecycle, and preserves the same completion/persistence contract. Focused timing, rollback, projection, output-cap, remote-routing, live-stream, and executable local/remote JSONL smoke tests cover the contract. |
| CLI | App-server background terminals | Implemented | Current Codex-compatible experimental `thread/backgroundTerminals/list`, `thread/backgroundTerminals/terminate`, and `thread/backgroundTerminals/clean` expose active local and remote thread-owned user shells by one stable app-server process ID, preserve raw command and working-directory metadata, report a real OS PID only for local processes, sort deterministically by process ID, paginate with Codex's signed-PID cursor semantics, terminate one process idempotently, and clean all active shells without interrupting unrelated thread work. Remote termination is acknowledged before its output reader is canceled; canceled long-poll responses are retired by exact request ID without resetting the shared exec-server connection. Termination state is shared with turn interruption and disconnect cleanup so a terminating process disappears immediately while its ordinary completion path still settles persistence and turn lifecycle exactly once. Focused concurrent local/remote shell XCTest, real WebSocket cancellation/late-reply tests, executable local and remote process round trips, and a source parity gate cover pagination, nullable OS metadata, live output, validation, cancellation races, connection reuse, termination, and cleanup. |
| CLI | App-server Guardian denial approval | Implemented | Codex 0.144.5-compatible `thread/approveGuardianDeniedAction` accepts current Guardian assessment events, acknowledges non-denied events without execution, and maps durable Auto-review lifecycle into `item/autoApprovalReview/started` and `item/autoApprovalReview/completed` notifications. A denied event is only an authorization claim: QuillCode requires an exact durable review ID, turn ID, target item ID, normalized action, available one-shot receipt, replayable arguments, and original user context. It reconstructs the call from private durable history, performs a fresh Auto review, persists progress and the consumed receipt before execution, and rejects forged, stale, redacted, concurrent, or replayed requests. Focused actor coverage proves exact shell execution and persistence; the executable JSONL smoke proves the method's public fail-closed boundary; a source parity gate binds runtime, tests, smoke, research, and decisions. |
| CLI | App-server standalone command execution | Implemented | Codex 0.142.5-compatible experimental `command/exec`, `command/exec/write`, `command/exec/resize`, `command/exec/terminate`, and `command/exec/outputDelta` support argv execution without a thread, relative or absolute cwd, inherited environment overrides and null unsets, pipe or PTY stdin, initial/updated PTY size, independent bounded or disabled capture, configurable/default/disabled timeout, timeout exit 124, deferred final response after all deltas, no streamed-output duplication, connection-scoped non-empty client IDs, ID reuse after exit, and process teardown without response leakage on disconnect. Built-in `:read-only`, `:workspace`, and `:danger-full-access` profiles plus explicit legacy sandbox policies enforce closed-by-default macOS Seatbelt or Linux bubblewrap boundaries; restrictive Linux execution fails closed if bubblewrap is unavailable. Managed `allow_upstream_proxy = false` strips inherited and request-supplied upstream proxy environment variables from `command/exec` and process launches before execution. Focused lifecycle/error/sandbox/network XCTest, existing process regressions including empty-ID rejection, an executable streamed JSONL round trip, and a source parity gate cover the contract. Arbitrary configured permission profiles, full managed proxy forwarding/domain routing, Windows sandbox behavior, and a remotely disabled local environment remain in the broader partial app-server row. |
| CLI | App-server client configuration discovery | Implemented | Codex 0.142.5-compatible `permissionProfile/list`, experimental `collaborationMode/list`, and `configRequirements/read` expose exact built-in profile order/pagination, collaboration presets, stable-versus-experimental requirement fields, recursive low-to-high managed TOML layering, hooks, network policy, computer-use policy, features, residency, approval policies/reviewers, sandbox/profile allowlists, and effective defaults. Managed restrictions are enforced on thread start/resume/fork, settings updates, and standalone command execution rather than being display-only. Persistence and actor tests cover merge/validation/projection/error behavior, the real JSONL smoke covers all three methods, and a source parity gate keeps runtime, tests, smoke, and research wired together. Custom configured permission profiles and remote MDM requirement adapters remain deferred in the broader partial app-server row. |
| CLI | App-server experimental feature catalog | Implemented | Codex 0.144.5-compatible `experimentalFeature/list` exposes canonical QuillCode flags with exact stage metadata, beta-only presentation copy, enabled/default state, zero-limit-safe cursor pagination, and loaded-task project-config refresh. `experimentalFeature/enablement/set` applies process-only patches to the explicitly runtime-capable subset and ignores unknown or non-runtime keys. Effective precedence is managed requirements, app-server CLI override, merged system/user/project config, runtime state, then code default. The `memories` runtime flag changes real model context without deleting durable notes; `hooks` remains truthfully discoverable but is not falsely advertised as runtime-mutable. Core, parser, actor, project-refresh, precedence, and model-context tests plus the executable JSONL smoke and a source parity gate cover the contract. |
| CLI | App-server hook discovery | Implemented | Current Codex-compatible `hooks/list` accepts up to 32 CWDs, defaults an empty request to the session CWD, and returns exact per-CWD hook metadata, warnings, and structured load errors without executing commands. Project, user, system, managed-requirements, and local plugin hooks share the same bounded data-only catalog used by desktop execution. Stable source keys, absolute source paths, feature policy, enabled state, exact-definition hashes, `managed`/`untrusted`/`trusted`/`modified` trust, and deterministic display order are preserved. Linked worktrees read project hooks from the primary checkout; malformed independent config/plugin layers remain isolated; path arrays and inline plugin hooks are supported; symlink, escape, type, byte, and count bounds fail closed. Focused catalog, plugin, worktree, and actor tests plus the executable JSONL smoke and a source parity gate cover the contract. Cloud/MDM plugin acquisition remains part of the broader partial plugin surface. |
| CLI | App-server Git diff to remote | Implemented | Codex 0.142.5-compatible `gitDiffToRemote` returns the current local upstream-tip SHA and one direct binary diff containing committed-ahead, staged, unstaged, and untracked changes while excluding ignored files. It preserves tracked-before-untracked ordering and upstream-tip rather than merge-base semantics without fetching or mutating the repository. External diff and text-conversion hooks are disabled; inventory, file count, elapsed process time, and aggregate diff bytes are bounded; unsafe paths and repositories without an upstream fail closed with the Codex `-32600` request boundary. Focused real-Git actor tests, a built-process JSONL smoke, research probes, and a source parity gate cover the contract. |
| CLI | App-server global memory reset | Implemented | Codex 0.142.5-compatible `memory/reset` accepts omitted parameters and idempotently clears only QuillCode's app-managed `~/.quillcode/memories` contents. It preserves the private root, rejects a file or symlink root, removes child links without following them, and never deletes project-owned `.quillcode/memories`. Focused persistence and actor tests, an executable JSONL smoke, research notes, and a parity gate cover the boundary. Already-injected context in an active turn is immutable; reset affects future memory loads. |
| CLI | App-server external-agent migration | Implemented | Codex 0.142.5-compatible `externalAgentConfig/detect`, `externalAgentConfig/import`, and `externalAgentConfig/import/readHistories` detect bounded home/repository Claude Code setup and recent sessions, return the import ID before progress, emit one per-item progress event, group completion in canonical service order, and persist newest-first history. Every item and selected detail is re-detected before mutation; imports serialize additive config updates, publish directories without overwrite, clean partial plugin copies, exclude static MCP/environment secrets, filter hook/MCP/session subsets exactly, reject source/history symlinks, and refresh config, skills, and MCP runtime state. Imported sessions become durable projects/tasks with provenance and workspace fallback. Focused persistence/actor tests, a real JSONL process smoke, research, decisions, and a parity gate cover ordering, partial failure, restart, concurrency, cancellation, and security boundaries. |
| CLI | App-server code review | Implemented | `review/start` accepts Codex 0.142.5 uncommitted, base-branch, commit/title, and bounded custom targets with inline-default or detached delivery. Inline reviews respond before streaming a normal turn plus entered/exited review-mode items on the source thread; detached reviews transactionally create a durable fork and emit `thread/started` before their response. Both paths reuse the native/CLI capability-limited reviewer without initializing MCP, accept `turn/interrupt`, persist tool evidence, and expose exactly one final assistant review from the validated `host.review.submit` report. Actor tests cover ordering, identity, target validation, detached ownership, capability restriction, interruption, historical-message isolation, and persistence; the real JSONL process smoke verifies inline lifecycle and final output. |
| CLI | MCP server tool bridge | Partial | `quill-code mcp-server` implements strict newline-delimited JSON-RPC 2.0 with the observed `2025-06-18` initialize/initialized handshake, exact public `codex` and `codex-reply` names and schemas, live-by-default or mock execution, string/integer IDs, durable start/reply threads, supported invocation/config overrides, bounded progress projection through `codex/event`, command and patch `elicitation/create` approvals, cooperative request cancellation, incremental persistence, and fail-closed EOF cleanup. Progress projection now includes assistant deltas, tool queued/running/completed/failed lifecycle, shell begin/end, typed `item_progress` updates with bounded message/completed/total/fraction fields, warnings, reasoning, and approval events. Patch approvals now include bounded path-keyed `codex_file_changes` metadata and the same map inside `codex_changes.changes` for `host.file.write` and `host.apply_patch`, preserving raw arguments while giving compatible clients a deterministic file-change summary before approval. Top-level MCP run arguments accept and advertise Codex-style kebab-case, snake_case, and camelCase aliases for approval policy, sandbox, base instructions, compact prompt, and developer instructions, rejecting conflicting aliases before a run is persisted. Config overrides accept a bounded Codex-style alias set for invocation-local model, sandbox, approval, review, spend, browser, Computer Use, notification, worktree, shortcut, skill, auth-mode, and API-base preferences while still rejecting account/secrets and unknown keys. Focused wire/catalog/config/session/approval/compaction/progress tests and `scripts/mcp-server-smoke.sh` drive the real executable through `whoami`, durable reply, event inspection, path-keyed patch metadata, persistence inspection, secret-leak rejection, and clean EOF. Full parity remains explicit: QuillCode projects a useful compatible event subset rather than every native Codex event variant; `on-failure` conservatively uses user review because QuillCode does not yet have Codex's OS-sandbox failure/escalation retry semantics; base instructions augment rather than replace the stable QuillCode structured tool contract; and arbitrary Codex config keys remain outside the allowlist. |
| UX | Codex desktop shortcuts | Implemented | The documented Codex desktop bindings are shared by SwiftUI menus, workspace commands, the command palette, Settings, and the Playwright harness: Command Palette (`Cmd+K`, `Cmd+Shift+P`), Settings (`Cmd+,`), Keyboard Shortcuts (`Cmd+Shift+/`), Open Project (`Cmd+O`), workspace Back/Forward (`Cmd+[` / `Cmd+]`), text scale (`Cmd++` / `Cmd+-`), Sidebar (`Cmd+B`), Open Review (`Ctrl+Shift+G`), Review panel (`Cmd+Option+B`), Bottom panel (`Cmd+J`), Terminal (`Ctrl+backtick`), Clear Terminal (`Ctrl+L`), Quick Chat (`Cmd+Option+N`), New Chat (`Cmd+N`, `Cmd+Shift+O`), Search Chats (`Cmd+G`), Find in Chat (`Cmd+F`), Previous/Next Chat (`Cmd+Shift+[` / `Cmd+Shift+]`), and Dictation (`Ctrl+Shift+D`). The native searchable shortcut editor switches between Action and Keystroke search, records custom bindings in app config, reports conflicts without mutation, resets one or all bindings, rejects unsafe bare typing keys, and recovers deterministically from malformed or conflicting hand-edited config. Primary bindings use native menus; only secondary aliases use the AppKit event monitor, keeping text entry and platform-reserved chords intact. Native model tests and Playwright interaction tests cover routing, aliases, customization, persistence, conflict handling, reset, Quick Chat, wrapped task navigation, review visibility, text scale, and dictation focus. |
| Core | Side conversations | Implemented | `/side [question]` and `/btw [question]` create an unpersisted history fork with an explicit mutation boundary. The fork can run while its parent task continues, stays out of sidebar projections and durable storage, disables incompatible lifecycle actions and subagents, and is discarded through a visible Return action without changing the parent transcript or draft. Native lifecycle, prompt, persistence, and true desktop-concurrency tests plus Playwright interaction coverage verify the contract. |
| Core | Personalities | Implemented | Friendly, Pragmatic, and None communication styles persist per chat and enter the agent request as a focused system-guidance layer. Settings chooses the default for future chats without rewriting existing overrides; `/personality friendly\|pragmatic\|none` changes only the current chat and writes a visible transcript confirmation. The composer hides personality commands when live model metadata explicitly reports no support, while absent metadata remains compatible because QuillCode provides the behavior in its own prompt layer. Forks, compaction, duplication, project/worktree creation, config/thread migration, and TrustedRouter live-catalog decoding preserve the contract. Focused Core, Agent, Persistence, App, and Playwright tests cover compatibility, prompt isolation, capability gating, scope, and interaction behavior. |
| Core | Threads and transcript persistence | Implemented | JSON thread store, desktop bootstrap loading, mock agent runner, per-chat draft and attachment persistence, stable app-server turn/client-message identities, history-only rollback across steering and restart, and independent concurrent agent runs keyed to explicit chat IDs. Switching chats leaves background work attached to its original project/worktree, progress and retry notices update that chat only, the sidebar keeps a compact live indicator, the selected composer reflects only its own run, and Stop All cancels every active chat run. |
| CLI | Non-interactive exec | Partial | `quill-code exec` provides live-by-default or deterministic mock execution, read-only/workspace-write/danger-full-access modes, Git-repository guarding, prompt and bounded untrusted stdin context, image attachments, ephemeral runs, exact/last task resume, atomic final-message output, bounded JSON Schema validation, and JSONL `thread`/`turn`/`item`/`error` lifecycle events with usage. Danger full access deliberately removes the built-in workspace path boundary for shell working directories and file tools in both exec and app-server, reports the `:danger-full-access` profile, and keeps safety review, edit guards, output caps, and secret protections intact. Global and project-scoped MCP configuration is loaded with the same precedence, filtering, stored OAuth, schema projection, and exact raw-name routing as app-server turns; ready tools are exposed as deterministic bounded `mcp__server__tool` definitions. Optional startup failures are isolated, while `required = true` failures stop before model invocation, attachment import, thread/turn start events, or transcript persistence and still emit the normal failure events. Every opened MCP process or connection is terminated after success, failure, runner-construction error, partial startup failure, or SIGINT, and `--ignore-user-config` skips both user and project MCP configuration. Focused XCTest plus process smoke cover shell execution, external paths, stdout/stderr separation, persistence, resume without historical-event duplication, malformed input, schema mismatch, JSON failure ordering, secret-output redaction, root-directory traversal termination, project MCP override precedence, exact MCP tool calls, required/optional failures, real child-process cleanup, and real SIGINT cancellation. SIGINT cooperatively cancels active model/tool work, persists the stopped partial task, exits 1 without a false successful terminal event, and skips final-output handling. |
| CLI | Doctor diagnostics | Implemented | `quill-code doctor` mirrors the observable Codex 0.142.5 diagnostic surface with `--json`, `--summary`, `--all`, `--no-color`, and `--ascii`. It checks installation/runtime provenance, config parsing, credential source, Git, terminal attachment, search support, MCP configuration, enforceable sandbox modes, state paths, a bounded 5,000-file/8 MiB-per-file task inventory, proxy-variable presence, TrustedRouter `/models` reachability, and app-server availability. Collection is read-only and does not create a missing home. URLs drop credentials/query/fragment; API keys, proxy values, MCP commands/headers/environment, malformed config text, and task content never enter output. JSON has a stable schema/check map and failure exit status; human output groups notes and sections with bounded list expansion. Parser/collector/renderer/corruption tests plus a real loopback process smoke prove authorization, redaction, report schema, human rendering, and zero state mutation. |
| CLI | Code review command | Implemented | `quill-code review` supports exactly one of `--uncommitted`, `--base BRANCH`, `--commit SHA` with optional `--title`, a custom criteria prompt, or `-` for bounded stdin. It is live by default with deterministic `--mock`, emits only the validated Markdown report on stdout and progress on stderr, remains ephemeral, and shares the desktop app's typed `QuillCodeReview` contracts, prompt builder, runner, normalization, and `host.review.submit` sink. The dedicated runner receives only bounded file/list/search and Git status/diff/branch inspection tools; shell, mutations, hooks, skills, web, Computer Use, subagents, attachments, and ordinary project tools are removed before execution. Parser/resolver tests, real temporary-repository command tests, native review regressions, a large-diff pipe-drain regression, and `scripts/cli-review-smoke.sh` cover all target forms, non-Git rejection, cancellation, title rendering, no persistence, typed completion, and fail-closed missing reports. |
| CLI | App server protocol | Partial | `quill-code app-server` implements the Codex-compatible stdio core against generated 0.142.5 schemas and observed wire behavior: strict initialize/initialized handshake, string/integer IDs, notification opt-outs, bounded incremental JSONL input, durable thread start/resume/fork/list/read/archive/unarchive/delete/name/goals plus transcript-backed `thread/search` and connection-local `thread/loaded/list`, cursor-paged `thread/turns/list` with `summary`/`full`/`notLoaded` views and reconnect-safe historical tool reconstruction, turn start/steer/interrupt, user/reasoning/assistant/tool item lifecycle and deltas, managed local images plus Codex `image` base64 data-URL inputs with preserved `auto`/`low`/`high`/`original` detail, configured safety-mode inheritance, command/file approval requests and responses, persistence, and EOF/interruption cleanup. Image bytes are MIME/magic/size validated, transactionally copied into private managed storage, removed when rejected or left in an interrupted steering queue, and never persist their inline data URL; raw HTTP(S) image URLs remain explicitly unsupported, matching current Codex core behavior. Codex `skill` and `mention` turn inputs round-trip as structured user-message content: selected skills must exactly match an enabled bounded catalog entry and persist an immutable model-context snapshot, while mentions preserve bounded identity without reading their path or claiming the deferred remote connector runtime. Experimental clients can use the complete connection-scoped standalone process lifecycle: argv-based `process/spawn`, inherited environment overrides/unsets, buffered or raw base64 streaming stdout/stderr, independent per-stream caps, explicit/default/unbounded timeouts, pipe and PTY modes, stdin writes/close, PTY resize, kill, handle reuse after exit, response-before-notification ordering, bounded output draining, and disconnect teardown. Discovery includes deterministic `model/list` pagination, truthful provider capabilities, non-secret `account/read`, API-key/browser TrustedRouter login, cancellation, logout, and ordered account notifications, locally observed UTC token usage with prompt/completion/context breakdowns, explicitly named local spend-control windows, structured `config/read` with per-leaf origins and optional raw user layers, atomic `config/value/write` and `config/batchWrite` with replace/upsert/delete semantics and content-version conflicts, bounded local `plugin/list` and `plugin/installed` marketplace projection with QuillCode package install/enable state, local `plugin/read` details plus local `plugin/skill/read` content for product-compatible namespaced skills, exact persisted hook keys, app metadata, and MCP names, bounded local `plugin/install` and `plugin/uninstall` with atomic cache replacement, package/symlink/size validation, immediate skill/hook discovery invalidation, app-auth summaries, and idempotent removal, Codex-compatible `marketplace/add`, `marketplace/remove`, and `marketplace/upgrade` for bounded local directories, GitHub shorthand, and HTTP(S)/SSH/local Git repositories with optional refs and sparse paths, transactional config/filesystem rollback, idempotent adds, selective/all upgrades, and per-marketplace upgrade errors, and shared Open Agent Skills metadata through `skills/list`, persistent path/name selectors through `skills/config/write`, bounded session-local `skills/extraRoots/set`, and recursive watched `skills/changed` invalidation with disconnect cleanup. Desktop and app-server marketplace discovery share one data-only, root-bounded reader; desktop and CLI runners enforce the same skill selectors. The host filesystem surface implements binary-safe `fs/readFile`/`writeFile`, recursive/default directory and remove semantics, exact metadata/directory shapes, overwrite-and-merge copy with symlink/special-file behavior, and connection-scoped `fs/watch`/`unwatch` with sorted 200 ms-debounced `fs/changed` notifications and disconnect cleanup. MCP parity includes exact-name `mcpServerStatus/list` with full or tools/auth-only inventory, thread-project config scopes, `config/mcpServer/reload`, lossless `mcpServer/tool/call`, `mcpServer/resource/read`, and asynchronous `mcpServer/oauth/login` over shared stdio or HTTP sessions with reload/EOF teardown. OAuth returns a real authorization URL, persists refreshable per-server credentials, emits a thread-aware completion notification, and reloads the registry on success. Ready MCP tools are also exposed to ordinary turns under deterministic bounded `mcp__server__tool` names with their exact input schemas and annotation-derived safety risk; execution routes back to exact raw names, emits native `mcpToolCall` lifecycle items, streams matching nonempty server messages through exact `item/mcpToolCall/progress` notifications, and refreshes the inventory on the next turn after reload. `required = true` servers initialize before thread start/resume/fork persistence and fail the operation if unavailable. Optional servers initialize after the lifecycle response and emit exact thread-scoped `mcpServer/startupStatus/updated` `starting` then `ready`/`failed`/`cancelled` transitions; reload cancels stale startup work, notification opt-outs do not suppress execution, and the current additive nullable `failureReason` field is preserved. Server-initiated MCP elicitation is bidirectional across stdio, Streamable HTTP, and legacy HTTP+SSE: standard typed forms, negotiated `openai/form` rich forms, and URL requests relay through exact `mcpServer/elicitation/request` server requests; accepted content and metadata return to the MCP server; malformed or unsupported requests fail closed; interruption and disconnect cancel pending work; and `serverRequest/resolved` precedes turn completion. Focused XCTest and real process smokes cover stdin-open JSONL transport, rich text/image/skill/mention input, actual MCP child launch, standalone process lifecycle, capability negotiation, all elicitation modes, response metadata, cancellation ordering, local plugin install/activation/idempotent uninstall, marketplace add/upgrade/remove, and a framed child-process round trip. Provider-owned quota history, provider-hosted plugin catalogs/sharing/install and remote `plugin/skill/read`, and the remaining full Codex app-server surface are deferred with explicit errors rather than silent no-ops. |
| Core | Import from another agent | Implemented | Settings exposes a native review-before-import flow for Claude Code projects, recent chats, instructions, settings, skills, plugins, MCP servers, hooks, slash commands, and subagents. Project and item selections remain editable before import; cancellation invalidates delayed discovery; imported setup is additive and destination-scoped; existing files are never overwritten; source symlinks, dependency trees, oversized files, and credentials are rejected or redacted; imported MCP credentials and hooks remain disabled pending explicit setup/trust; and destination-scoped receipts prevent duplicate work while allowing global setup to be applied to a project added later. Imported instruction files participate in normal project context locally and over SSH. Projects, chats, newly-created artifacts, and receipts commit as one rollback-safe transaction. Persistence, workspace integration, native coordinator, and Playwright tests cover discovery, review, import, project/thread/receipt failure rollback, cancellation, deduplication, secret exclusion, and follow-up presentation. |
| Core | Project picker/sidebar | Partial | Workspace state model, persisted project registry, selected-project context, native project rail, Codex-like New chat/Search primary nav, quieter Workspace footer for Plugins/Automations/Terminal/Browser/Memories/Activity/Command palette, desktop folder picker, current-directory desktop project seed, typed Local/SSH Remote project connections, project row actions for New chat, Refresh context, Move to top, Move up, Move down, Move to bottom, Rename, and Remove from list, SSH Remote Refresh context over the existing SSH executor, thread selection, rename/duplicate/pin/archive/unarchive/delete menus, bulk select/select-all/pin/unpin/archive/unarchive/delete actions with inactive Select inside the Chats filter menu and one visible Done action during selection, pinned/recent/archived chat grouping before the lower Projects section, stable All/Pinned/Recent/Archived saved filters with counts and filtered bulk-selection behavior behind progressive disclosure, typed custom saved searches with counts and persisted create/delete/reorder management in that menu, compact thread rows with title plus relative activity and searchable/help-accessible model metadata, compact one-line project rows with help-accessible full paths, whole-row native SwiftUI project drag/drop including drop-to-end, shared project-list count/order/local-remote summaries in SwiftUI and the Playwright harness, row accessibility labels, project-specific action-menu labels/help, hover/focus/open action-menu emphasis, shared audited click-target and clearance contracts, scoped select-all behavior, archived-state search, command-palette and `/project top|up|down|bottom|list` slash routing, Codex-style `Cmd+B` sidebar visibility toggling through the shared command registry, and Playwright coverage exist. Packaged macOS smoke now AX-presses Search and New Chat: Search proves automatic field focus plus reversible native text entry, while New Chat proves exactly one chat is created and selected, its composer is immediately focused and typeable, and prior workspace state is restored. Broader packaged-window pointer automation and remaining Codex workspace-list edge cases are pending. |
| Core | TrustedRouter runtime | Partial | CLI and desktop can use live TrustedRouter when an env or stored secret key exists; CLI auth key management and native developer settings exist; desktop has an explicit OAuth-first auth mode, loopback PKCE sign-in action, pure authorize/exchange/userinfo OAuth client, persisted delegated key storage, non-secret signed-in account display, and developer override as a secondary mode; desktop refreshes the live model catalog when keyed and preserves whether the picker is showing bundled fallback, live TrustedRouter catalog data, or fallback after a refresh failure. A proactive desktop refresh coordinator now refreshes the keyed catalog at startup, refreshes stale live catalog metadata on a bounded interval, backs off briefly after refresh failures, and treats env-provided TrustedRouter keys the same as stored keys. Agent runs now expose incremental thread progress, consume live TrustedRouter action text as a stream, normalize common near-miss tool-call aliases back to canonical schema keys, recover provider wrapper dialects such as `tool_calls`, `function_call`, `tool_use`/`input`, Responses-style `output`, and stringified JSON `arguments` before they reach execution, show Streaming status while chunks arrive, render safe `say` action text as a live assistant draft without leaking tool JSON, support bounded multi-step tool continuation with hidden tool-feedback messages, surface actionable runtime issue cards/pills for missing sign-in, missing developer key, rejected key, rate limit, provider outage, network failure, empty response, malformed model action, and generic run failure, can retry the latest user turn through the same send path, opens the searchable model picker directly from malformed-response/rate-limit/provider-outage recovery, routes model-backed auxiliary context-summary and compaction calls through a cheap catalog-selected model without changing the main thread model, shows redacted runtime diagnostics plus parsed provider, rate-limit, HTTP status, request-id metadata, model-catalog source/freshness, catalog-derived provider-health summary, and typed recovery route/reason telemetry in Settings, derives local Today/Week/Month spend rows from persisted priced run receipts in the top-bar budget meter, and can render optional local day/week/month spend caps from config as `$spent / $cap` rows without pretending they are provider quotas. The desktop also fetches the authenticated provider-owned current balance from TrustedRouter's `/credits` endpoint through the Swift SDK, refreshes it at startup and on a bounded cadence, retains the last known value with an explicit stale warning after transient failures, and exposes a manual refresh in Settings plus a distinct compact top-bar balance. The balance is not persisted and is never labeled as a quota or local spend estimate. Provider account history and provider-owned day/week/month quota or reset-window APIs remain pending. |
| Core | TrustedRouter model picker | Partial | Catalog model, live fetch adapter, searchable grouped provider/category/model SwiftUI picker, keyboard result highlighting with ArrowUp/ArrowDown plus Enter selection, persistent user favorites, recent-model surfacing from thread history, current/default/recommended/favorite badges, deterministic provider/category/model/default/recommended/favorite metadata rows, inline provider-detail browsing, duplicate-free normal search over structured row metadata, shared model-row accessibility labels for provider/category/state/capability context, model-specific details/favorite action labels, shared category section counts plus bounded provider summaries for large provider/category result groups, live TrustedRouter capability metadata for context window, pricing, modalities, capabilities, status, and summary, first-party branded capability taxonomy for Nike/Zeus/Prometheus/Socrates/Aristotle/Plato fallback rows, retired raw synth names rejected as typed `/model` targets/defaults, unbranded fallback provider discovery rows such as MiniMax M3 for offline/failed-refresh search, explicit bundled/live/fallback catalog freshness labels in the picker and Settings, catalog-aware no-results guidance that tells the user whether search is limited by bundled/fallback data or a live TrustedRouter catalog in both the picker and inline `/model` composer search, no-match `/model` drafts block Return/Send instead of dispatching typos as model IDs, catalog-derived provider-health summary in the picker and Settings, compatibility decoding for older surface payloads, proactive desktop catalog refresh for stale provider-health metadata, and Playwright coverage exist. Packaged macOS smoke now AX-presses the real composer model control, proves automatic search focus plus reversible AXValue entry, and requires the identified Prometheus 1.0 catalog row without changing the selected model. Broader provider-category polish and upstream capability-taxonomy expansion remain pending. |
| Core | Image input | Implemented | macOS can select up to four PNG/JPEG/GIF/WebP images per turn, preview and remove them in the composer, send image-only or text-plus-image turns, persist unsent and sent attachments per thread, preserve them through thread switches/retry/follow-up queues, and render sent thumbnails in SwiftUI, static HTML, Markdown export, and the Playwright harness. Selected images are copied into private QuillCode-owned storage; TrustedRouter transport revalidates root containment, regular-file metadata, byte count, and magic bytes before serializing OpenAI-compatible multimodal `image_url` content. Unit, integration, and Playwright tests cover legacy decoding, symlink escape rejection, multimodal request shape, image-only send, removal, and queued drain. |
| Tools | Shell commands | Implemented | `host.shell.run` with empty-command guard, workspace-bounded `cwd`, bounded `timeoutSeconds`, and bounded structured `environment` overrides. Local projects execute in the selected workspace; SSH Remote projects execute agent shell calls through noninteractive SSH from the remote root and reject unavailable local-only tool calls instead of silently touching the desktop workspace. |
| Tools | File read/list/write/search | Implemented | Workspace-scoped UTF-8 file read/write, bounded immediate directory listing via `host.file.list`, and bounded literal workspace search via `host.file.search`, with file/line/preview matches, directory/file kind metadata, dependency/build-directory skips, result caps, artifact links, per-thread read-before-overwrite guards for model-authored writes, explicit user-commanded write exemptions for high-confidence immediate actions, no-op write rejection, file-lock serialization, natural-language immediate action planning, tool-card subtitles, deterministic CLI smoke, Playwright real-world evidence, and opt-in live TrustedRouter smoke coverage. Missing-file reads now return bounded deterministic sibling suggestions, sanitize odd filenames into single-line diagnostics, and directory reads point models/users to `host.file.list`. |
| Tools | Apply patch | Implemented | Workspace-scoped unified diff application through `git apply`, with per-thread read-before-patch guards for existing files, file-lock serialization, symlink/root boundary checks, C-quoted/rename/copy diff header path parsing, and surfaced hunk-level `git apply` diagnostics when a patch cannot be applied. |
| Tools | Git status/diff/stage/restore/commit/push/PR/worktrees | Partial | Status/diff, fetch, fast-forward-only pull, branch list, branch switch/create, file-level stage/restore, hunk-level stage/restore, commit, conservative named-remote branch push, GitHub PR listing via structured `gh pr list`, GitHub PR creation via structured `gh pr create`, GitHub PR view/comments via `gh pr view --comments`, GitHub PR checks via `gh pr checks`, PR diff via `gh pr diff`, PR checkout via `gh pr checkout`, top-level GitHub PR comments via `gh pr comment`, GitHub PR review submissions via `gh pr review`, inline GitHub PR review comments via `gh api` with resolved PR/repo metadata, inline comment replies via `gh api`, review-thread listing plus resolve/unresolve via `gh api graphql`, reviewer requests/removals via `gh pr edit --add-reviewer/--remove-reviewer`, label add/removal via `gh pr edit --add-label/--remove-label`, PR close/reopen via `gh pr close|reopen`, PR merge/automerge via `gh pr merge`, and git worktree list/create/open/remove/prune are implemented locally and for SSH Remote projects with workspace/path/patch/ref-name/branch-name/branch-start-point/PR-list-state/PR-list-limit/PR-selector/checkout-branch/comment-body/lifecycle-action/review-action/review-comment-line/review-reply/review-thread-list/review-thread/reviewer/label/merge-method checks. Managed local tasks start detached in an app-owned configurable root (`~/.quillcode/worktrees` by default), support transactional Local/Worktree Handoff for uncommitted state and clean fast-forward-only committed history, expose **Create branch here** to claim a validated branch in place, and save repository-bound restorable snapshots before disposable worktrees are removed on archive or recent-task cleanup. Detached managed tasks also expose **Finish task in Local**, which verifies Handoff into Local, removes the registered checkout without force, clears the binding only after success, and leaves a retryable Local cleanup state when concurrent edits prevent removal. Cleanup is enabled by default with a Codex-compatible 15-task limit, can be disabled or resized in Settings, and never removes pinned, selected, running, Local, or named-branch worktrees; each binding persists its original authorized root so later settings changes cannot orphan or broaden cleanup authority. Branch ownership is persisted and disables both Create branch and Handoff. Named local worktree branches expose **Publish branch**, which verifies branch ownership, clean state, base progress, upstream divergence, and existing-PR state before routing push/create/view through ordinary audited tool cards. The task then persists exact PR identity and lifecycle state, exposes **Land** through audited squash auto-merge, refreshes GitHub as the authoritative source, and offers non-force cleanup only after a merged PR, clean checkout, matching branch, and exact published head are all reverified; task and PR history remain after the worktree binding is removed. Handoff rejects dirty history destinations, backward moves, and divergence without intentional mutation. Dedicated model code review now preflights local or SSH Remote Git state, runs a capability-limited read-only reviewer, requires one strict `host.review.submit` report, and renders validated P0-P3 findings in the transcript and Review pane without exposing mutation actions on finding-only files. SSH Remote apply-patch/review actions refresh remote diff after successful changes. PR review submissions now have a structured review-draft panel for approve/comment/request-changes actions, can include, skip, and edit each saved line/range review note before posting selected notes as inline GitHub review comments ahead of the final review submission, show a live submit summary for action/target/body/inline-note readiness, and PR review-thread browse/select is surfaced from completed tool results with direct Resolve/Unresolve and inline Reply actions that refresh the thread list. Inline-note reorder controls preserve user-chosen review-comment order through draft state, rendered UI, and final tool-call dispatch. Automatic, explicit No setup, and named project worktree environments are implemented with platform-specific script resolution and persisted task selection. |
| Safety | Read-only/Review/Auto modes | Implemented | Auto can use static or model reviewer; SwiftUI shell can switch and persist modes. Static Auto review also approves bounded single-command read-only diagnostics for identity, date/time, hostname, OS/kernel, uptime, process list, memory, and disk usage when the latest user request asks for that exact diagnostic class, while shell composition, environment dumps, and unrelated tool families remain gated. |
| Safety | Reviewer model call | Partial | TrustedRouter client, native OAuth/developer settings state, desktop loopback OAuth wiring, and structured reviewer telemetry exist. Auto review records whether approval came from static policy, primary reviewer model, fallback reviewer model, or permission rules, plus attempted models and bounded fallback diagnostics in durable approval payloads. The reviewer prompt pins explicit approve/clarify/deny boundaries for bounded user-requested work, missing/empty arguments, mismatched targets, credential exfiltration, unrelated chained shell actions, broad destructive work, persistent security weakening, and irreversible disk/account changes. Reviewer parsing accepts raw JSON or a whole-response fenced JSON block while rejecting prose-wrapped JSON. Completed reviews now persist typed outcomes and canonical redacted action identities; `/approve`, `/approvals`, and `/denials` expose the newest ten denials without creating a model turn. An eligible denial can retry the exact original action once with fresh IDs and mandatory re-review, while private/redacted calls, changed turn/workspace/mode context, consumed retries, and repeated-denial circuit breakers fail closed. Core, Agent, native persistence, command-routing, static-renderer, and Playwright lifecycle tests cover denial, reopen, exact retry, second denial, successful execution, crash-safe consumption, and duplicate-dispatch refusal. Broader production calibration against live reviewer-model transcripts remains pending. |
| UX | Tool cards | Partial | Tool-card presentation model, HTML harness, and SwiftUI shell render queued/running/done/failed/review states with a shared density contract: queued/running cards peek, completed cards collapse, routine ready-to-run approval cards peek with visible Run/Edit/Skip actions, and failed or blocked review cards expand when the user needs details; project-bound tool cards carry shared execution-context metadata and render local/SSH Remote chips plus a thin context rail so old transcript scrollback still says where work ran; agent progress callbacks update the selected thread while a run is in flight; completed tool calls also produce human final-answer bubbles for common actions while full raw output stays behind card details; transcript rendering now interleaves messages and tool cards chronologically from the event timeline and preserves scroll intent across appends; hidden model-facing tool feedback is filtered from transcript surfaces, sidebar search, fork seeds, and compaction summaries; tool-result artifacts are surfaced as visible file/URL preview chips, local source/text artifacts render bounded UTF-8 previews, Markdown artifacts render document-style preview cards while retaining bounded raw text previews for local files, image-like artifacts render bounded previews, PR links render from structured artifacts, and PDF/document/spreadsheet/presentation/appshot artifacts render structured preview cards in SwiftUI, static HTML, and the Playwright harness; live TrustedRouter runs surface Streaming status before queued tools/final answers; safe streamed `say` text updates the assistant bubble before finalization; stopped queued/running tools close as failed `Stopped by user` cards; messages and tool cards expose lightweight Copy/Copy output actions with visible `Copied` feedback, the whole visible conversation can be copied or exported as Markdown through the shared transcript exporter, user messages can be reused as focused composer drafts, assistant messages intentionally omit Helpful/Not helpful ratings because QuillCode does not collect response feedback, and the latest assistant response exposes a Retry action that reuses the normal retry command in SwiftUI, static HTML, and the Playwright harness. The Playwright harness now enforces first-pass interface polish primitives for smoothing, wrapping, tabular counters, visible and unblocked hit areas, semantic click-target classes, transitions, press feedback, radii, card density, image outlines, execution-context chips/rails, bottom-anchored empty state, and mid-scroll/bottom-pinned transcript append behavior. Native SwiftUI action buttons now use QuillCode-owned primary/secondary/destructive action styles instead of compact platform button styles, keeping the visible surface, 40 pt native hit target, disabled state, and 0.96 press feedback together across settings, banners, dialogs, secondary panes, terminal, and artifacts. Native hit-target smoke now also inventories interaction surface families, including persistent chrome, sidebar rows, transcript controls, tool cards, context banners, command palette, search, settings, model picker, review, secondary panes, and menu-bar actions. Packaged live-window smoke now adds real AXPress activation proof for New Chat, Search, Settings, Automations, and Extensions, with typed before/after state and deeper focus/text-entry evidence preserved in release artifacts. Broader native QuillUI polish and full embedded document/media renderers pending. |
| UX | Activity/task pane | Partial | A right-side Activity pane is derived from the selected thread event log, tool cards, active instructions, memories, artifacts, latest assistant answer, deterministic task plan, optional model-authored `host.plan.update` task plan, deterministic handoff fallback, optional model-authored `host.handoff.update` handoff summary with next steps, optional model-authored `host.subagents.update` progress for explicit parallel-agent workflows, and real `/subagents objective | Name: role` scheduler progress emitted through that same tool/event contract. SwiftUI, static HTML, and the Playwright harness share the same surface contract, including command-driven collapsible Task Plan/Recent/Subagents/Handoff/Tools/Sources/Artifacts/Latest Answer sections, and tests cover task/source/tool/artifact/plan/handoff/subagent rendering plus collapse behavior. Scheduler-level cancellation renders cancelled progress, and cancelled slash-command runs preserve stopped state without fake success summaries. Subagent workers run isolated multi-step sessions through the configured normal-chat session factory, inheriting project/worktree/model/mode/instructions/memories/goal plus local or SSH routing; progress remains pinned to the originating chat when selection changes. The scheduler supports dependency waves, blocked/skip/cycle handling, hierarchical paths, bounded concurrency, and bounded recursive `[[DELEGATE: name | role]]` spawning. Each worker exposes a bounded credential-redacted Activity projection while scheduler-owned workers persist private full child transcripts for current and historical drilldown. Review-gated workers pause independently; exact generation-bound Run/Skip actions consume owner-only held calls once, resume only the affected dependency graph, and reject stale or duplicate replay. Compact parent manifests survive relaunch, interrupted workers are never replayed implicitly, and migration-era whole-session journals remain readable without becoming the production format. The native pane exposes an identified Close control and task summary; packaged macOS smoke opens it through AXPress, verifies real content, dismisses through Close, and proves the composer regains the pane's horizontal space. Broader Activity density and non-subagent workflow parity remain pending. |
| UX | Review pane | Partial | Latest completed `host.git.diff` is summarized into a changed-file pane in SwiftUI and the Playwright harness. A compact Unstaged/Staged/Commit/Branch/Last turn scope control keeps successful empty results visible with truthful scope-specific copy. Commit shows one exact validated commit patch; Branch compares a validated base branch to `HEAD` through Git's merge base; both historical scopes retain their reference in replayable tool state and expose only read-only Open actions. Last turn derives only the newest user turn's recorded `apply_patch` events, coalesces repeated edits by file, never falls back to an older turn, and discloses when other mutating tools make its provenance partial; remote Last turn stays read-only. Unstaged files and hunks expose Stage/Restore, staged files and hunks expose only non-destructive Unstage, and focused Stage all/Revert all/Unstage all controls pass exactly the visible paths through validated local or SSH Git execution. Last turn Revert all reuses the atomic reverse-patch engine. Local and SSH Remote projects share the same command semantics and scope-aware action plan. Successful `host.apply_patch` runs immediately refresh `host.git.diff` so edits flow into review; binary and deleted changed-file rows omit the non-readable Open action and show a concise unreadable reason; file-scoped notes plus line and range review comments are replayed from thread events and hidden when stale. The command-palette Review pull request action opens a structured PR review draft inside the review pane with approve/comment/request-changes actions, optional selector, body validation, direct `host.git.pr.review` submission, a live submit summary for action/target/body/inline-note readiness, and an editable pending inline-note queue that lets users include, skip, and edit each saved line/range note before batching selected notes through `host.git.pr.review_comment` ahead of the final review. Completed `host.git.pr.review_threads` results render a PR review-thread section with location, status, snippet, and 44 pt Resolve/Unresolve plus inline Reply controls; reply posting executes `host.git.pr.review_reply` then refreshes the thread list. Structured PR review submissions, inline GitHub PR review comments, review replies, review-thread listing, and review-thread resolve/unresolve actions can also be posted from agent tools and `/pr review`, `/pr review-comment`, `/pr review-reply`, `/pr review-threads`, or `/pr review-thread`; inline-note reorder controls keep the submitted comment order explicit. |
| UX | Top bar widget | Partial | Top-bar presentation state, HTML harness, SwiftUI shell, and macOS `MenuBarExtra` track thread/project/model/mode/status/Computer Use and expose quick New Chat, Open Project, Command Palette, Terminal, Browser, Settings, Stop All, and real Disconnect All affordances for active MCP servers and selected SSH Remote projects. The main workspace top bar is intentionally quiet: one-line thread identity leads, Codex-style workspace Back/Forward controls restore prior thread/project selections through shared command routing, status is a dot/status popover, overflow is a single ellipsis with a bounded utility menu for Command Palette, Search, Computer Use setup, Settings, Keyboard Shortcuts, and Disconnect All only when it can act, while long instruction/memory/Computer Use/project labels remain bounded metadata instead of a dashboard row. Model selection and Auto/Review/Read-only now live beside the composer as send-time controls in SwiftUI, static HTML, and the Playwright harness, with model/mode kept independent. The top bar projects active queued/running/review-gated tool work from the transcript reducer and durable thread-goal state into compact SwiftUI/HTML/accessibility chips without placing long objective text in the one-line chrome. Stop All cancels active sends, integrated-terminal commands, and active MCP servers; Disconnect All shares that cleanup and also detaches selected SSH Remote context without removing the project. Agent-router process cancellation, QuillCloud remote controls, and Linux tray adapter pending. |
| UX | Context and rate banners | Partial | Context pressure banner appears near the local budget and offers Compact context, New thread, Fork from last, Fork summary, and Fork full. Compact context creates a bounded continuation thread while preserving project/mode/model/instructions/memories; fork controls can now carry the latest visible turn, a live TrustedRouter summary plus latest turn when signed in, a deterministic summary fallback plus latest turn when offline or summary generation fails, or the full visible transcript while hiding internal tool-continuation messages. TrustedRouter streaming runs request provider token usage, persists the latest usage as structured thread metadata, and prefers that exact count over character estimates when deciding whether to show the banner. Parsed rate-limit guidance from provider headers is preserved across runtime error seams and projects into top-bar quota rows such as remaining quota and reset/retry windows, so users can see why the agent is blocked without digging into Settings diagnostics. Priced local run receipts now also roll up into Today/Week/Month top-bar spend rows, giving users live spend-history visibility before a richer TrustedRouter account endpoint exists. Model-backed compact/fork summary recovery now records source-thread notices and continuation-thread telemetry for `model` vs `deterministic_fallback`, summary length, and redacted fallback diagnostics; Activity now promotes those notices into a dedicated Context section with progress/outcome rows across SwiftUI, static HTML, and Playwright. Compact/fork-summary actions now render an in-banner running strip with status copy and temporarily disable competing context-move actions while the continuation summary is being prepared. Richer provider account-history and period-specific provider quota APIs remain pending. |
| UX | Keyboard shortcuts/slash commands | Partial | Local slash command parser handles `/help`, `/status`, `/goal objective|status|complete|block|resume|clear`, `/new`, `/clear`, `/undo`, `/rename title`, `/duplicate`, `/pin`, `/unpin`, `/archive`, `/unarchive`, `/delete`, `/fork last|summary|full`, `/project open|new|refresh|top|up|down|bottom|list|rename|remove`, `/ssh user@host:/path`, `/compact`, `/stop`, `/retry`, `/back`, `/forward`, `/history back|forward`, `/mode`, `/model`, `/terminal`, `/terminal clear`, `/search`, `/find`, `/focus`, `/sidebar`, `/copy`, `/export`, `/settings`, `/shortcuts`, `/commands`, `/extensions`, `/skills`, `/skill name`, `/automations`, `/activity`, `/browser [target]`, `/review`, `/git fetch`, `/git pull`, `/branch`, `/branch switch`, `/branch create`, `/worktrees`, `/worktree create|open|remove|prune`, `/pr create|view|checks|diff|checkout|comment|review|review-comment|review-reply|review-threads|review-thread|reviewers|labels|close|reopen|merge`, `/memories`, `/remember text`, `/remember-edit memory-id`, `/forget memory-id`, and `/env [name]`; workspace-action slash commands share the same model/tool paths as visible UI and the command palette, and Git sync/branch, PR, and worktree slash subcommands dispatch through the same structured local/SSH Remote `host.git.*` tools as agent calls. `/search`, `/find`, `/focus`, `/sidebar`, `/copy`, `/export`, `/stop`, `/retry`, `/back`, `/forward`, `/history back|forward`, `/settings`, `/shortcuts`, `/commands`, `/extensions`, `/skills`, `/automations`, and `/activity` open or run the same global Search, active-chat Find, composer focus, sidebar toggle, conversation copy/export, Stop All, Retry last turn, workspace Back/Forward, Settings, Keyboard Shortcuts, Command Palette, Extensions/Skills, Automations, and Activity surfaces as visible commands and shortcuts, while staying view-level so they do not fail as model-only workspace dispatches. `/browser` without a target toggles the browser pane, while `/browser target` and `/preview target` open URLs, localhost apps, or project files through the same resolver as the browser address bar. `/skill name` stays a normal agent turn that loads and follows the named installed skill. `/project open` shares the same Open Project route as the visible project picker, while project list/reorder/remove slash commands share the project rail and command-palette mutation paths. `/review` opens the dedicated model code-review scope chooser; `/diff` opens the ordinary Git comparison pane. Both remain available through the same command registry as their visible actions. `/clear` resets the selected chat transcript, tool/event history, queued follow-ups, and draft while preserving the thread shell, project, mode, model, instructions, memories, and pinned/archive state. `/undo` reverses the latest currently revertable local turn's recorded `apply_patch` edits through the same honest reverse-patch engine as the transcript Revert button, while staying disabled when no local revert plan exists. `/pin` and `/unpin` idempotently update the selected chat's pinned state through the same persisted lifecycle path as sidebar row actions, with archived chat pinning disabled. `/delete` removes the selected chat through the same persisted thread-delete command path as the command palette and sidebar row action, with copy clarifying that project files are untouched. `/fork` routes selected-chat continuations through the same context fork strategies as the banner and command palette: latest turn, model/deterministic summary, or full visible context. `/goal` attaches durable objectives to the selected chat, projects active/blocked/completed state into the top bar, persists with the thread, and feeds the objective into the TrustedRouter prompt. The composer now shows filtered slash suggestions from the same catalog as `/help`, with keyboard selected-row navigation, Enter/Tab accept behavior, click-to-insert command skeletons, and multiline textarea behavior with Shift+Enter newlines plus Enter-to-send in SwiftUI, static HTML, and the Playwright harness. Search opens a finder over thread title, model, pinned/archived state, and indexed transcript/tool content with focused input, highlighted results, ArrowUp/ArrowDown navigation, and Enter selection in SwiftUI and the Playwright harness. Active chat Find is implemented as a `Cmd+F` transcript bar with result counts, next/previous navigation, focused input, active-result highlighting, and command-palette access. A grouped command palette over workspace commands exists in SwiftUI and the Playwright harness, with registry-backed shortcuts, keyword/shortcut/title ranking, selected-row keyboard navigation, `>` action scoping, `/` slash-command scoping with visible scope badges, and slash-template insertion that returns focus to the composer; E2E coverage verifies primary workspace actions and scoped slash insertion. `Cmd+/` opens a command-derived Keyboard Shortcuts panel in SwiftUI, desktop menus, menu bar, and the Playwright harness. Workspace history has registry-backed `Cmd+Option+←` / `Cmd+Option+→` shortcuts plus `/back`, `/forward`, and `/history back|forward` aliases that stay separate from browser `Cmd+[` / `Cmd+]`. Complete Codex shortcut parity pending. |
| Workspace | Integrated terminal | Partial | Workspace-scoped terminal pane, command history with Up/Down recall that preserves partially typed drafts, live stdout/stderr streaming for running commands, local terminal commands launched through a pseudo-terminal so `isatty()`/prompt echo/interactive stdin behave like a real terminal, persisted execution context per terminal entry, persistent per-project working directory and exported/unset environment deltas for local projects, SSH Remote terminal execution through noninteractive `ssh -T -o BatchMode=yes`, remote cwd/env continuity across terminal commands through stripped marker metadata, explicit running/done/failed/stopped command lifecycle, terminal-local Stop/Suspend/Resume, Clear history, `/terminal clear`, Stop All integration, cancellable command execution, native SwiftUI surface, static HTML rendering, Playwright coverage, readable ANSI/OSC output cleanup, and a single-page cursor-addressed renderer for common TUI/status redraw sequences exist. The renderer now also handles bounded scroll regions, C0 vertical-tab/form-feed line controls, non-printing C0/DEL stripping, index/next-line/reverse-index escapes, C1 IND/NEL/RI line-control forms, RIS and DECSTR terminal resets, DECALN screen alignment, DEC origin mode, explicit scroll up/down, insert/delete line, insert/delete/erase/repeat character, relative cursor-position aliases (`HPR`/`VPR`), default and custom tab stops (`HTS`/`TBC`), C1 CSI/OSC control forms, DCS/PM/APC/SOS string-control stripping, alternate-screen latest-frame preservation, width-two CJK/emoji/emoji-presentation advancement, ZWJ grapheme-cluster preservation, combining-mark cursor accounting, narrow-by-default ambiguous-width policy for stable transcript scrollback, automatic wide ambiguous-width policy for CJK locale terminal surfaces, a `QUILLCODE_TERMINAL_AMBIGUOUS_WIDTH` override for support/debugging, and SGR style runs for standard/bright/256/RGB foreground and background colors plus bold, faint, italic, underline, inverse, conceal, and strikethrough. Styled runs preserve cursor-overwrite and alternate-screen semantics and render in native SwiftUI, static HTML, and the Playwright harness. Live local PTY apps can enable DEC button/button-motion/any-motion tracking (`1000`/`1002`/`1003`) with X10, UTF-8, SGR, or URXVT encoding (`1005`/`1006`/`1015`); native left/middle/right click, drag, hover, and bounded trackpad/mouse-wheel events are mapped to one-based terminal cells and delivered only while the negotiated mode remains active. Running local PTYs also receive printable text, control/Option chords, arrows, Home/End, Page Up/Down, Insert/Delete, Tab/Shift-Tab, Escape, Enter, Backspace, and F1-F12 through a focused platform adapter. DEC application-cursor (`1`) and bracketed-paste (`2004`) modes are parsed incrementally; stale-mode requests are rejected and paste payloads are bounded with embedded boundary sequences removed. Real PTY and Playwright tests prove exact keyboard/pointer protocol delivery and lifecycle reset behavior. Full curses-style semantics and a richer terminal emulator surface remain pending. |
| Workspace | Worktrees | Partial | Core `git worktree` list/create/open/remove/prune tools, shared command-palette actions, create/open/remove dialogs, open/remove dialogs with known-worktree choices from `git worktree list --porcelain`, local and SSH Remote picker loading/error/retry states without transcript tool-card noise, safe dry-run cleanup command, review/confirm stale-worktree prune dialog, `/worktree create|open|remove|prune` slash commands, create/open-time handoff into a selected local or SSH Remote worktree project/thread, thread-bound run-root isolation, and top-bar worktree status/warning chips for isolated or dangling bound threads exist. New local Worktree tasks now use transactional Codex-style managed materialization: they start at detached HEAD, preserve staged and unstaged tracked edits separately, copy bounded untracked files, copy only normally ignored files selected by `.worktreeinclude`, automatically copy ignored `AGENTS.override.md`, skip source symlinks, refuse overwrites, and remove the new worktree if transfer fails. Detached managed tasks support transactional Local/Worktree Handoff of staged, unstaged, and nonignored untracked state while preserving one task and one stable associated worktree; matching commits accept an exact destination snapshot without reapplying, while committed history advances only by a verified fast-forward into a clean destination. Backward moves, divergence, different repositories, dirty history destinations, and concurrent source mutation fail without intentional source cleanup, and failed transfers restore history QuillCode advanced. They can claim a validated branch in place with **Create branch here**, or use **Finish task in Local** to hand the task back to Local and remove the registered checkout without force. Named worktree branches expose **Publish branch** in the compact top bar and command palette; publication refuses dirty or upstream-diverged state, avoids redundant pushes, refreshes an open PR, or pushes with upstream tracking and opens a filled PR through visible tool cards. Published tasks persist exact PR identity and head state, expose audited auto-merge landing, refresh queued status after relaunch and task selection, and offer non-force post-merge cleanup only after GitHub confirms the merge. Existing worktrees are never removed by background reconciliation; only an already-missing merged binding is repaired automatically. Finish clears the durable binding only after cleanup succeeds; concurrent edits leave the task in Local with an explicit **Finish worktree cleanup** retry command. Archiving an unpinned, idle, detached app-owned task atomically saves its staged patch, unstaged patch, and bounded safe local files before removing the registered worktree; the persisted chat keeps a compact snapshot reference and offers one-click restore at the exact captured commit. Restore rejects repository/path/manifest drift and rolls back a partially created checkout on apply failure. Pinned tasks, running tasks, Local handoffs, and named-branch worktrees are never removed automatically. Managed roots are configurable, recent-task retention defaults to 15, and new managed tasks present Automatic, No setup, and project-defined named environment choices. Automatic uses the configured default named environment when present or the bounded platform-specific/fallback setup script; named selections resolve their own platform scripts, and the choice persists with the task. Setup executes through the visible shell tool-card path, while missing named environments fail visibly without discarding the checkout. Native/HTML top bars, sidebars, command palette, persisted thread state, and Playwright expose active, saved, missing, Local, restored, setup, branch-publish, PR landing, merge-queue, merged-cleanup, finish-confirmation, and cleanup-retry states. Richer branch comparison and merge-queue administration remain pending. |
| Workspace | Local environment actions | Partial | Project-local `.quillcode/actions/*.sh` and `.quillcode/local-env/*.sh` scripts are discovered with symlink/root bounds and exposed in the command palette, `/env [name]`, and a contextual top-bar **Actions** group as `Run ...` actions that dispatch through `host.shell.run`. The native menu and HTML/Playwright harness reuse the same command IDs, hide the group when no action can run, and preserve tool-card auditing and environment-value redaction. Optional project `.quillcode/config.toml` can add bounded local action directories and cap discovered action count while keeping the default action roots available. Optional same-name JSON sidecars provide display `title`, `description`, `order`, bounded `environment`, safe project-relative `workingDirectory`, and bounded `timeoutSeconds` metadata without changing which script executes; command search includes environment key names, cwd paths, and timeout labels, not values. Local-env commands pass cwd, environment, and timeout as structured tool arguments instead of embedding shell-quoted command prefixes. Tool-card/transcript payloads keep environment key names while redacting values, and reruns read current project metadata instead of recovering secrets from thread history. Direct shell `cwd` arguments are normalized and rejected unless they resolve inside the selected workspace, `timeoutSeconds` is bounded to 1-1800 seconds, and direct `environment` overrides use the same key/value policy. `/env schedule ...` creates persisted local-environment action automations with the same natural scheduling grammar as follow-ups/workspace checks, due runs execute the selected action from refreshed project metadata, and action/schedule matching rejects ambiguous prefix collisions. Richer local-env orchestration remains pending. |
| Workspace | AGENTS/rules instructions | Partial | Project-local and nested `AGENTS.md`, `.quillcode/rules.md`, and `.quillcode/instructions.md` are bounded, persisted on project refs, copied into thread context in broad-to-specific order, injected into TrustedRouter as hidden system context, and surfaced as a top-bar status. Instruction records carry derived applicability scopes, the prompt tells the model to apply scoped rules only under matching paths, and Activity sources show the applicable scope plus visible duplicate-scope, nested-overlap, explicit nested-override, and conservative semantic-conflict diagnostics for explicit opposing instructions over overlapping scopes. Additive nested instructions are no longer flagged just because broader instructions exist. Conflicts are promoted into a first-class Activity Instruction Review section with issue counts and toggle controls while remaining visible in Sources for audit continuity. Activity instruction rows expose typed Open/Edit actions, conflict diagnostics expose line-specific Open Source/Edit Source plus direct keep-side patch actions for deterministic two-line semantic conflicts, exact duplicate same-scope diagnostics expose direct Clear duplicate actions, repeated broad lines inside nested instructions expose direct Remove repeated lines actions, explicit nested override diagnostics expose line-specific Remove override lines actions when the loaded excerpts still match, Resolve/Dismiss actions, and precise Resolve drafts with `file:line` source targets, excerpts, and suggested edits. Keep-side, nested-overlap, and explicit override-line patch actions run through `host.apply_patch`; exact duplicate Clear actions run through `host.file.write` with explicit empty content; all refresh project context after success and let disappearing diagnostics record as resolved-by-edit audit records. Dismissed diagnostics persist as project-scoped resolution records. Local desktop source actions open readable workspace files in the native editor at the exact line when possible and fall back to model file-read/edit-draft behavior when a project is remote, missing, unreadable, or outside the workspace. Non-identical duplicate merges and semantic nested override rewrites remain pending. |
| Browser | In-app browser | Partial | Workspace browser panel, address normalization for web/localhost/file/project-relative targets, persisted browser allowed-domain/blocklist policy in Settings, policy enforcement for manual opens, `host.browser.open`, snapshot fetches, live-DOM final URLs, redirect final URLs, and visible browser-session updates, bounded local HTML metadata snapshots, bounded HTTP(S) HTML snapshot fetching for reachable web/localhost pages, explicit static-vs-network/live-DOM inspection-depth labels, visible page outlines, text snippets, web/localhost source summaries, browser comments, Back/Forward/Reload snapshot-history controls with shared `Cmd+[`, `Cmd+]`, and `Cmd+R` shortcuts in SwiftUI, desktop menus, static HTML, and Playwright, structured `host.browser.open` and `host.browser.inspect` agent tools, focused browser-tab state with isolated history/snapshot/comment projection, native/static HTML/Playwright tab creation, switching, and closing, stale async fetch protection across tab switches, a shared browser-session sync snapshot, reverse visible-session update contract, macOS visible WebKit tabs that present/sync the current browser tab set, report tab selection/title/URL changes back into the preview model, and send rendered live-DOM snapshots from completed visible-session navigations through the same snapshot model as offscreen captures, a live DOM capture adapter contract, macOS desktop WebKit live-DOM capture for rendered HTTP(S) pages, persistent WebKit browser-profile reuse for cookies/session state by default, explicit ephemeral capture mode for future privacy/test controls, a reusable macOS visible WebKit browser-session window wired from the browser pane, menu bar, command palette, and `/session [target]` slash command for signing into sites with the same persistent profile, app-driven visible-session Back/Forward/Reload so `Cmd+[`/`Cmd+]` prefer the open WebKit tab history with model-sync fallback and `Cmd+R` refreshes the open WebKit tab instead of only resyncing unchanged model state, tested macOS visible-session JavaScript evaluation, selected-tab live DOM capture, desktop `host.browser.inspect` preference for the open visible WebKit session with snapshot fallback, selector-safe `host.browser.click`, `host.browser.type`, and bounded `host.browser.script` tools routed through the open visible WebKit session, Linux browser-process live-DOM capture through headless Chromium/Chrome `--dump-dom` when a browser is installed, native desktop smoke evidence for local HTML browser open/comment/`host.browser.inspect`, and Playwright coverage exist. Linux visible-session control remains pending. |
| Computer Use | Backend protocol | Partial | Backend protocol, deterministic stub, shared platform backend factory, Linux session/helper detection, macOS permission-aware status, screenshot capture, mouse, keyboard, and scroll adapter exist. The desktop window and menu bar render specific missing-permission labels, Settings includes an actionable Computer Use card with Screen Recording, Accessibility, setup, refresh commands, and state-specific onboarding steps for missing permissions, backend-helper failures, refresh-after-permission changes, and foreground app approvals; agent runs can dispatch `host.computer.screenshot`, click, type, scroll, move, and key tools through the active backend with concise final answers. Screenshots are stored once as private per-thread managed artifacts instead of raw base64 in transcript details, render as image previews on tool cards, carry foreground-app metadata when the backend can identify it, include a bounded visual-summary descriptor with size, foreground app, preview artifact filename, and macOS accessibility labels for visible controls when Accessibility is granted, and continue to the selected image-capable model as hidden multimodal tool feedback so the next action can inspect actual pixels. The prompt requires a fresh capture after screen-changing actions and treats screenshot text as untrusted page content. Linux has helper-backed screenshot/input routing for Wayland (`grim` + `ydotool`/`wtype`) and X11 (`import`/`scrot` + `xdotool`) when those helpers are installed. Computer Use enforces optional foreground-app approvals by bundle identifier or app name before input/screenshot tools run, and Settings exposes a focused approval-management card that edits the same persisted config. Settings can quick-add the detected foreground app by bundle identifier when available or by app name when needed. Richer packaged Accessibility-driven interaction smoke and advanced recovery remain pending. |
| Plugins | Record & Replay | Implemented | macOS Extensions and the command palette expose **Record a skill**, which focuses a normal composer draft before the agent requests `host.workflow.record.start`. Cross-app recording always requires one explicit confirmation that cannot be bypassed by Auto reviewer output or persisted permission rules. The recorder observes bounded app switches, clicks, scrolling, text/special-key activity, and private resized screenshots; protected Accessibility fields retain only character counts. Capture is capped at 30 minutes, 240 events, and 12 snapshots, private artifacts use owner-only permissions, and queued events are rejected once stop begins. **Stop** remains immediate even if permissions are revoked, is available in Extensions and the menu-bar widget, and returns the capture to its originating task across task switches. Raw capture telemetry stays in hidden model feedback, and the ordinary audited agent path creates exactly one `.quillcode/skills/<slug>/SKILL.md` with purpose, variable inputs, generalized steps, and verification. Unit, safety, native integration, static-renderer, and Playwright mock-LLM tests cover the lifecycle. Codex documents this feature as macOS-only; Linux recording is therefore not required for official feature parity. |
| Plugins | Skills/plugins/MCP | Partial | Project-local `.quillcode/plugins/*.json`, `.quillcode/skills/*.json`, and `.quillcode/mcp/*.json` manifests are discovered with symlink/root and byte/count bounds, surfaced in the Extensions pane, command palette, static HTML renderer, macOS menu bar, and Playwright harness. Standard local Codex plugin packages under `.quillcode/plugins/<plugin>/.codex-plugin/plugin.json` are also discovered without executing package code: bounded package-relative `skills` and `mcpServers` references project bundled skills and namespaced MCP servers into the same Extensions inventory, direct project skills retain precedence, enabled plugin skills are advertised to and loadable by the live agent, and stdio MCP launch expands `${CODEX_PLUGIN_ROOT}` through direct argv/environment values from a revalidated package working directory. Standard repo marketplaces at `.agents/plugins/marketplace.json` plus the legacy `.claude-plugin/marketplace.json` location discover bounded `./` local package sources, honor unavailable policy and catalog precedence, render them as Available, and install through a typed atomic copy tool that revalidates package identity, boundaries, size, symlinks, and destination collisions without running lifecycle scripts. Project-local `.quillcode/marketplace/*.json` catalog entries remain compatible and install through the auditable shell tool-card path. A small bundled marketplace catalog exposes curated available skills for LLM Advisor, Browser Use, OpenClaw Video Toolkit, and BurstyRouter; each bundled install writes both the skill directory and a matching `.quillcode/skills/*.json` manifest so project manifests with the same ID shadow the bundled entry after refresh. Manifests can expose version/source metadata and explicit bounded install/update commands that run through normal shell tool-card auditing, then refresh project metadata. MCP stdio manifests preserve structured command/args and can be explicitly started/stopped from the Extensions pane or command palette. Start performs a bounded MCP `initialize` plus `tools/list` probe before showing Ready, optionally probes advertised `resources/list` and `prompts/list`, surfaces server/protocol/tool/resource/prompt metadata including bounded per-tool input schema summaries, and Stop All terminates active MCP servers. Ready MCP servers expose generic allowlisted `host.mcp.call`, `host.mcp.resource.read`, and `host.mcp.prompt.get` agent tools that return MCP content into the normal tool-card/final-answer flow, and the dynamic tool description includes each ready MCP tool's required/optional argument schema. Agent-authored MCP calls now attach a per-call progress token, consume bounded monotonic `notifications/progress` over stdio, Streamable HTTP, or legacy HTTP+SSE, coalesce replayable transcript snapshots, update SwiftUI/HTML/CLI progress surfaces, and clear the card immediately on completion, failure, or cancellation. Ready MCP resources and prompts are also directly actionable from the Extensions pane and command palette, dispatching through the same live MCP runtime, transcript tool cards, and final-answer rendering as agent-authored calls. Trusted standard `UserPromptSubmit`, `Stop`, `PreToolUse`, `PostToolUse`, `PermissionRequest`, `PreCompact`, `PostCompact`, `SessionStart`, `SubagentStart`, and `SubagentStop` command hooks are wired through typed bounded execution paths. Project, user, and system `.quillcode` plus Codex-compatible `.codex` JSON/TOML definitions merge with plugin and managed-requirements hooks through one bounded decoder, scoped exact-definition trust store, Hooks surface, and execution pipeline. System and managed hooks are immutable policy; user hooks are globally reviewed; managed-only and feature-pin policies are enforced; and local global hooks stay local for SSH Remote workspaces. Signed git/npm marketplace distribution, personal marketplace acquisition, app/connectors, cloud/MDM managed-document delivery, additional event families, prompt/agent handlers, and asynchronous hooks remain pending. |
| Long-running | Durable thread goals | Implemented | A bounded typed goal model persists with each chat, survives relaunch, compaction, forks, and duplication, remains protected from stale in-flight agent snapshots, and is injected into TrustedRouter as explicit system context until completed. `/goal objective`, `/goal status`, `/goal complete`, `/goal block reason`, `/goal resume`, and `/goal clear` share one typed lifecycle engine and local transcript path. Active, blocked, and completed state projects into the native/static top bar with the full objective and blocker available through help/accessibility detail. |
| Long-running | Automations/subagents | Partial | Automations have a first-class command, sidebar entry, SwiftUI/static HTML/Playwright pane, `QuillAutomation` model, JSON persistence, desktop bootstrap loading, command-palette and slash creation for bounded relative/calendar recurrences, thread follow-ups, workspace checks, file/directory/Last-Modified/feed monitors, manual and due runners, recurring `nextRunAt` advancement, native macOS and Linux notifications, alert settings, and pause/resume/delete actions. Explicit subagent workflows share a replayable `host.subagents.update` schema, Activity section, `/subagents` command, and parallel dependency scheduler with cancellation, bounded concurrency, hierarchical paths, same-group dependency shorthand, and bounded recursive spawning. Natural-language/model-authored delegation uses structured `host.subagents.run` to launch that real scheduler, persist live parent manifests, surface queued/running/completed workers, and return bounded summaries for one-turn parent consolidation. Workers use the normal configured multi-step runtime with file, shell, Git, browser, Computer Use, MCP, memory, local-worktree, and SSH Remote routing. Production persistence uses compact restart-safe run manifests, separate private child transcripts, and owner-only approval payloads; raw child transcripts never enter parent model feedback, direct children cannot launch independent scheduler trees, and relaunch reconciliation never implicitly replays interrupted work while exact generation-bound Run/Skip actions resume only the affected graph. Migration-era whole-session state is accepted only through the compatibility adapter. Richer recursive-subagent controls and autonomous background scheduling remain pending. |
| Memory | Memories/Chronicle | Partial | Local memories load from `~/.quillcode/memories` and project `.quillcode/memories`, with extension allow-listing, symlink/root escape rejection, count/file/total byte caps, truncation labels, thread snapshots, TrustedRouter system-context injection, top-bar status, Memories pane, static HTML, Playwright coverage, slash-command visibility, command-palette Add memory prefill, `/remember text` explicit global writes, `/remember-edit` explicit global updates plus local and SSH Remote project-memory updates from memory-card Edit, agent-callable `host.memory.remember` writes for user-requested durable preferences/facts, Forget actions for global/local project/SSH Remote project memories, transcript/tool audit records, immediate context refresh, remote memory edits and deletes bounded to already loaded `.quillcode/memories` files over SSH with post-mutation context refresh, credential/token/password/private-key rejection for writes and edits with redacted transcript/tool/thread-title payloads, deterministic Memories-pane redaction review rows with Add safe memory actions in SwiftUI, static HTML, and Playwright, and deterministic Memories-pane conflict review for obvious opposing global/project notes with edit actions in SwiftUI, static HTML, and Playwright. The native pane has identified Add and Close controls, and packaged macOS smoke opens it through a real AXPress route, verifies rendered content, dismisses it through Close, and preserves memory state. Idle Chronicle jobs, broader project-memory review workflows, deeper redaction workflows, and fully autonomous memory inference without explicit user intent remain pending. |
| Remote | Phone/SSH remote | Partial | `ProjectConnection` persists typed local/SSH references. The sidebar, command palette, and Settings open one native connection dialog that discovers concrete aliases from a bounded, cycle-safe `~/.ssh/config`/`Include` scan, resolves display metadata through OpenSSH's effective `ssh -G` configuration, supports filtered selection or validated manual entry, probes the remote folder before registration, preserves the configured alias so `ProxyJump`/`IdentityFile` continue to apply, and provides explicit loading, retry, validation, failure, cancellation, and success states. `/ssh user@host:/path` remains as a compatible fast path. The sidebar shows an `SSH Remote` badge, terminal labels render the remote address, integrated terminal commands run over noninteractive SSH from the remote root, agent `host.shell.run`, bounded `host.file.read`/`host.file.list`/`host.file.write`, `host.apply_patch`, `host.git.status`/`host.git.diff`, file/hunk `host.git.stage`/`host.git.restore`, `host.git.commit`, conservative `host.git.push`, `host.git.pr.create`/`view`/`checks`/`diff`/`checkout`/`comment`/`lifecycle`/`review`/`review_comment`/`review_reply`/`review_threads`/`review_thread`/`reviewers`/`labels`/`merge`, and `host.git.worktree.*` calls route through the same remote command planner, and explicit Refresh context loads bounded remote `AGENTS.md`, `.quillcode/rules.md`, `.quillcode/instructions.md`, and project `.quillcode/memories` files into the selected thread context. Agent tool calls preferentially reuse one persistent `quill-code app-server --stdio` launched through SSH in the remote login shell. App-server startup failures may fall back to one-shot SSH only before dispatch; transport loss after dispatch reports an unknown execution state without replaying potentially mutating work. The agent receives only remote-safe tools for SSH Remote projects so unavailable tools cannot accidentally run locally, and remote shell/file/patch/git tool cards plus terminal entries show per-item SSH execution-context chips/rails. Provider-hosted remote approvals, QuillCloud/relay project execution, and full remote thread handoff remain pending. |
| Artifacts | Tool-result artifact links and previews | Partial | Tool-result artifacts now render as visible file/URL preview chips with labels and source details in SwiftUI, static HTML, and the Playwright harness. Local source/text file artifacts render bounded UTF-8 previews with binary, byte, line, and extension guards plus structured type, line-count, file-size, and truncation metadata, including common project and component formats such as Vue, Svelte, Astro, C#/F#, PHP, npm/package and TypeScript config manifests, Cargo/Python/Composer manifests, Gradle/Kotlin scripts, and Go module/checksum files. Image-like file, web, and inline data artifacts render bounded visual previews with structured type/extension/source metadata below the chips, and multi-image tool outputs show capped sequence labels so generated batches remain inspectable; local PNG/GIF/JPEG/SVG/BMP/WebP/TIFF/ICO file artifacts add bounded header-derived pixel dimensions to the preview type line without network fetches or full image decoding. PDF, Markdown/MDX, document, spreadsheet, presentation, audio, video, archive, and appshot artifacts render structured type/detail/open cards with shared model derivation; `.appshot.json` bundles are classified as Appshots rather than noisy raw JSON previews. Local Markdown and MDX artifacts parse a bounded UTF-8 prefix for first heading, heading count, file size, and truncation metadata while retaining the bounded raw text preview for inspectable content. Local JSON artifacts parse bounded UTF-8 files into compact root-kind, key/item count, file-size, and top-key metadata while retaining the bounded raw text preview for inspectable content. Local Jupyter notebook artifacts parse bounded `.ipynb` JSON metadata and render notebook version, language, code/markdown/raw cell counts, and file size without executing cells or rendering outputs. Local JSON Lines/NDJSON artifacts parse bounded line-oriented UTF-8 records into compact format, record-count, observed-key, file-size, and truncation metadata while retaining the bounded raw text preview for inspectable content. Local TOML artifacts use the same strict TOML document decoder as QuillCode config persistence to render bounded top-level-key, table, array, value, file-size, and key-list metadata while retaining the bounded raw text preview for inspectable content. Local PDF artifacts now parse bounded metadata and render embedded title, PDF version, page count, file-size metadata, native SwiftUI/PDFKit first-page previews, and static HTML/Playwright local `application/pdf` first-page preview objects without fetching remote PDFs or extracting full text. Local Office Open XML document/spreadsheet/presentation artifacts inspect bounded ZIP central-directory metadata only, rendering package-entry counts, worksheet/slide counts, file-size labels, and capped package-content labels such as document body, sheets, and slides without decompression or remote fetches. Local RTF document artifacts parse a bounded 64 KB prefix for title, encoding, file size, and truncation metadata without shelling out to rich-text converters or rendering raw control text. Local HTML document artifacts parse a bounded 64 KB prefix for title, first H1/H2 heading, link/script/style counts, file size, and truncation metadata while retaining the bounded raw source preview. Local diff/patch artifacts parse a bounded 128 KB prefix for changed-file, hunk, addition/deletion, file-size, and truncation metadata while retaining the bounded raw source preview. Local ZIP and uncompressed TAR archives render entry count, top-level item count, bounded first-entry summaries, capped structured content lists, and file-size metadata without extraction; ZIP archives reuse the bounded central-directory reader and TAR archives read bounded 512-byte headers only. Local single-member GZIP archives and GZIP-wrapped `.tar.gz`/`.tgz` archives parse bounded headers/trailers for original filename, compressed size, and uncompressed size without decompressing payloads. Local XZ and XZ-wrapped `.tar.xz`/`.txz` archives validate bounded container magic/footer bytes and infer the wrapped member label from the filename so common compressed code bundles get truthful non-extracting previews. Local BZIP2/Zstandard single-member archives plus `.tar.bz2`/`.tbz`/`.tbz2` and `.tar.zst`/`.tzst` wrappers validate bounded magic bytes and infer wrapped member labels without decompression. Local audio/video artifacts render bounded format and file-size metadata, local MP3 files parse bounded ID3 title/artist frames for richer cards, static HTML/Playwright previews expose native browser audio/video controls through bounded local file URLs without streaming remote media, and SwiftUI media cards render local AVPlayer-backed audio controls plus native video playback instead of wrapping players inside a broad Open link. Local font artifacts validate bounded TTF/OTF/TTC/WOFF/WOFF2 headers and render format, flavor, table count, declared size, and file-size metadata without parsing font tables or decompressing webfont payloads. Local executable/library artifacts validate bounded ELF, Mach-O, and PE headers for `.bin`, `.dll`, `.dylib`, `.exe`, `.o`, and `.so` files and render format, architecture, bitness, endian, and file-size metadata without executing, loading, or disassembling binaries. Local CSV/TSV artifacts parse a bounded UTF-8 prefix and render compact table previews with capped rows, columns, cell text, and truncation labels in SwiftUI, static HTML, and Playwright surfaces without fetching remote tables. Appshots parse bounded local `.appshot.json` metadata and render embedded title, summary, app, viewport, window count, captured timestamp, screenshot-thumbnail previews, and capped replay/history action, frame, and event timelines in SwiftUI, static HTML, and Playwright surfaces. Full embedded renderers for docs, sheets, slides, archive extraction/listing beyond bounded ZIP/TAR/GZIP/XZ/BZIP2/Zstandard summaries, and richer generated-media history remain pending. |

## Current Parity Notes

- Artifact previews now include bounded local YAML/YML metadata using the existing Yams parser:
  root kind, top-level keys, sequence/mapping/value counts, file size, and capped key previews.
- Artifact previews now include bounded local Apple property-list metadata using Foundation parsing:
  root kind, key/item counts, dictionary/array/value counts, file size, and capped key previews.
- Artifact previews now include bounded local SQLite database metadata for `.db`, `.sqlite`, and
  `.sqlite3` files by validating the 100-byte SQLite header and rendering page size, page count,
  and file size without opening tables or shelling out.
- Artifact previews now include bounded local WebAssembly metadata for `.wasm` files by validating
  the 8-byte module header and rendering module version plus file size without disassembly or
  shelling out.
- Artifact previews now include bounded local HTTP Archive metadata for `.har` browser/network traces:
  HAR version, creator, entry count, observed methods, status groups, host previews, and file size
  without rendering request/response headers, cookies, bodies, or query values.
- Artifact previews now include bounded local LCOV coverage-report metadata for `lcov.info` and
  `.lcov` files: source-file counts, line/branch/function coverage, file size, truncation state, and
  capped source-file previews without executing test tooling or following report paths.
- Artifact previews now include bounded local Go coverage-profile metadata for `cover.out` and
  `coverage.out` files: mode, source-file counts, block counts, statement coverage, file size,
  truncation state, and capped source-file previews without running `go tool cover` or following
  covered source paths.
- Artifact previews now include bounded local SARIF static-analysis metadata for `.sarif` and
  `.sarif.json` files: SARIF version, run/result counts, level counts, file size, capped tool labels,
  and capped rule labels without fetching remote reports or following result paths.
- Artifact previews now include bounded local CycloneDX SBOM metadata for `.json` files whose
  `bomFormat` validates as `CycloneDX`: spec version, root component, component/service/dependency
  counts, vulnerability severity counts, file size, and capped component labels without expanding
  license text, hashes, evidence, or fetching remote SBOMs.
- Artifact previews now include bounded local SPDX JSON SBOM metadata for `.json` files whose
  `spdxVersion` validates as SPDX: document name/namespace, package/file/relationship counts,
  extracted-license and creator counts, file size, capped package labels, and capped license labels
  without expanding extracted license text, checksums, snippets, relationships, or fetching remote
  SBOMs.
- Artifact previews now include bounded local npm lockfile metadata for `package-lock.json` files:
  lockfile version, root package, package/dependency/dev/optional counts, file size, capped package
  labels, and capped resolved registry hosts without expanding integrity hashes, package scripts, or
  fetching remote tarballs.
- Artifact previews now include bounded local Composer lockfile metadata for `composer.lock` files:
  plugin API version, content-hash prefix, package/dev-package counts, file size, capped package
  labels, and capped source hosts without expanding install scripts, autoload metadata, full hashes,
  or fetching Packagist/source archives.
- Artifact previews now include bounded local Go checksum metadata for `go.sum` files: module,
  version, checksum, and go.mod-checksum counts, file size, capped module labels, and capped source
  hosts without validating hashes through the Go checksum database or fetching module sources.
- Artifact previews now include bounded local Python requirements metadata for `requirements.txt`
  and `requirements-*.txt` files: package, pinned/ranged/editable/include/option/hash counts, file
  size, capped package labels, and capped source hosts without running pip, validating hashes,
  expanding requirement includes, or fetching package indexes.
- Artifact previews now include bounded local Poetry lockfile metadata for `poetry.lock` files:
  package/version/dev/optional/source/hash counts, file size, capped package labels, and capped
  source labels without expanding dependency tables, validating hashes, reading package metadata, or
  fetching package indexes/distributions.
- Artifact previews now include bounded local Pipfile lockfile metadata for `Pipfile.lock` files:
  default/develop/pinned/editable/source/hash counts, file size, capped package labels, and capped
  source labels without expanding dependency graphs, validating hashes, reading package metadata, or
  fetching package indexes/distributions.
- Artifact previews now include bounded local uv lockfile metadata for `uv.lock` files:
  Python requirement, package/version/dependency/source/hash counts, file size, capped package labels,
  and capped source labels without expanding dependency graphs, validating hashes, reading package
  metadata, or fetching package indexes/distributions.
- Artifact previews now include bounded local Bundler lockfile metadata for `Gemfile.lock` files:
  Bundler version, gem/dependency/platform/source counts, file size, capped gem labels, and capped
  source labels without expanding dependency graphs, validating checksums, reading gem metadata, or
  fetching gem indexes/distributions.
- Artifact previews now include bounded local CocoaPods lockfile metadata for `Podfile.lock` files:
  CocoaPods version, pod/dependency/source/checksum counts, file size, capped pod labels, and capped
  source labels without expanding dependency graphs, validating checksums, reading podspecs, or
  fetching spec repos/distributions.
- Artifact previews now include bounded local pnpm lockfile metadata for `pnpm-lock.yaml` files:
  lockfile version, importer/package/dependency/integrity counts, file size, capped importer labels,
  capped package labels, and capped resolved registry hosts without expanding dependency graphs,
  integrity values, package manifests, registry indexes, or fetching remote tarballs.
- Artifact previews now include bounded local SwiftPM resolved-package metadata for `Package.resolved`
  files: schema version, pin/version/branch/revision-only counts, file size, capped pin labels, and
  capped source hosts without expanding full revisions, dependency source bodies, or fetching remote
  repositories.
- Artifact previews now include bounded local Yarn lockfile metadata for `yarn.lock` files:
  package/version/resolution/integrity counts, file size, capped package labels, and capped registry
  host labels without expanding dependency graphs, integrity/checksum values, package manifests, or
  fetching remote registries/tarballs.
- Artifact previews now include bounded local Cargo lockfile metadata for `Cargo.lock` files:
  package/version/source/checksum counts, file size, capped package labels, and capped source labels
  without expanding dependency arrays, full checksums, manifests, crates, or fetching remote
  registries/repositories.
- Artifact previews now include bounded local font metadata for `.ttf`, `.otf`, `.ttc`, `.woff`,
  and `.woff2` files by validating fixed headers and rendering format, flavor, table count,
  declared size, and file size without parsing tables or decompressing webfont payloads.
- Artifact previews now include bounded local executable/library metadata for `.bin`, `.dll`,
  `.dylib`, `.exe`, `.o`, and `.so` files by validating ELF, Mach-O, or PE headers and rendering
  format, architecture, bitness, endian, and file size without executing or disassembling binaries.
- Artifact previews now include bounded local Jupyter notebook metadata for `.ipynb` files:
  notebook version, language, code/markdown/raw cell counts, and file size without executing cells
  or rendering notebook outputs.
- Artifact previews now include bounded local XML metadata using native XML parsing:
  root element, element/attribute/namespace counts, file size, and capped root-child previews.
- Artifact previews now include bounded local JUnit XML test-report metadata for `.xml` files whose
  root is `testsuite` or `testsuites`: suite/test counts, failure/error/skipped counts, duration,
  file size, suite labels, and failing-test labels without expanding testcase logs or following paths.
- Artifact previews now include bounded local Visual Studio TRX test-report metadata for `.trx`
  files: run name, test outcome counts, duration, file size, and capped failing test names without
  expanding failure output or fetching remote reports.
- Artifact previews now include bounded local xUnit.net XML test-report metadata for `.xml` files
  whose root is `assemblies` or `assembly`: assembly/collection/test counts, pass/fail/skip counts,
  duration, file size, capped assembly names, and capped failing test names without expanding failure
  output or fetching remote reports.
- Artifact previews now include bounded local NUnit XML test-report metadata for `.xml` files whose
  root is `test-run`: run name, test outcome counts, duration, file size, and capped failing test
  names without expanding failure output or fetching remote reports.
- Artifact previews now include bounded local Cobertura XML coverage-report metadata for `.xml` files
  whose root is `coverage`: package/class counts, line/branch coverage, file size, package labels,
  and class labels without executing coverage tooling, expanding source files, or following paths.
- Artifact previews now include bounded local Clover XML coverage-report metadata for `.xml` files
  whose root is `coverage` plus Clover project/metrics markers: package/file/class counts,
  element/method/statement/conditional coverage, file size, project labels, and file labels without
  executing coverage tooling or following paths.
- Artifact previews now include bounded local JaCoCo XML coverage-report metadata for `.xml` files
  whose root is `report` plus JaCoCo package/session/counter markers: package/source/class counts,
  line/branch/method/class coverage, file size, package labels, and source-file labels without
  executing coverage tooling or following paths.
- Artifact previews now include bounded local Istanbul/nyc JSON coverage-report metadata for
  `coverage-final.json` and `coverage-summary.json` style artifacts: source-file counts,
  line/statement/branch/function coverage, file size, and capped source-file previews without
  executing coverage tooling, following report paths, or fetching remote JSON.
- Artifact previews now include bounded local coverage.py JSON metadata for `coverage json`
  artifacts with `meta`, `files`, and `totals`: source-file counts, line/branch coverage, file size,
  and capped source-file previews without running coverage.py, following report paths, or fetching
  remote JSON.
- Artifact previews now include bounded local pytest JSON report metadata for pytest-json-report
  artifacts: total/pass/fail/error/skip counts, exit code, duration, file size, and capped failing
  test node IDs without expanding captured logs or fetching remote JSON.
- Artifact previews now include bounded local Jest/Vitest-style JSON report metadata for `.json`
  files with Jest-compatible `numTotalTests`/`testResults` shape: result, runtime, test/suite
  counts, file size, and capped failing assertion labels without expanding failure messages or
  fetching remote JSON.
- Artifact previews now include bounded local TAP report metadata for `.tap` files: plan, assertion
  counts, pass/fail/skip/TODO counts, bailout reason, file size, and capped failing assertion labels
  without expanding diagnostics or fetching remote reports.
- Artifact previews now include bounded local INI-style config metadata for `.ini`, `.cfg`, and `.conf`:
  section/key counts, file size, truncation state, and capped section previews.
- Artifact previews now include bounded local dotenv metadata for `.env` and `.env.*` files:
  variable/export counts, file size, truncation state, and capped variable-name previews without
  rendering raw dotenv values in artifact cards.
- Artifact previews now include bounded local RTF document metadata:
  title, encoding, file size, and truncation state without running rich-text conversion tools.
- Artifact previews now include bounded local HTML document metadata:
  title, first H1/H2 heading, link/script/style counts, file size, and truncation state while
  retaining the bounded raw source preview for inspectable content.
- Artifact previews now include bounded local diff/patch metadata:
  changed-file, hunk, addition/deletion, file-size, and truncation counts with capped changed-file
  labels while retaining the bounded raw source preview for inspectable content.
- Source artifact previews now recognize common repo/build control files such as Docker ignore,
  EditorConfig, Prettier config, CMake, Justfile, Taskfile, Bazel BUILD/WORKSPACE, Nix, Meson,
  Homebrew, Procfile, pnpm, and Yarn files with specific labels in SwiftUI and the Playwright
  harness.
- Archive artifact previews now include bounded local 7z/RAR container cards by validating
  signature bytes and rendering truthful format/size metadata without extraction or synthetic
  entry listings.
- Archive artifact previews now classify common ZIP-container deliverables (`.jar`, `.war`,
  `.ear`, `.apk`, `.ipa`, `.epub`, `.whl`, `.vsix`, `.xpi`, and `.nupkg`) and reuse the bounded
  central-directory reader to render exact package-format labels, entry counts, capped contents,
  and file sizes without extraction.
- The app server exposes Codex-compatible response-first `thread/compact/start`.
- The app server exposes Codex-compatible one-shot and live-session fuzzy file search with bounded
  shared indexing, exact wire fields, match scores/indices, cancellation, and disconnect cleanup.
  It emits a non-steerable `contextCompaction` turn, reuses the desktop's
  live/mock compaction policy and compact hooks, persists progress transactionally,
  rejects concurrent turns, and can be interrupted without mutating history after
  cancellation. Focused protocol, hook, persistence, failure, and executable smoke
  tests cover the wire contract.
- The workspace review pane now models Unstaged, Staged, Commit, Branch, and Last turn as explicit scopes
  instead of treating every diff as an implicit working-tree view. Scope changes
  dispatch the matching structured `host.git.diff`, successful empty results
  remain visible with a truthful scope-specific state, and refreshes preserve
  the selected scope. Staged rows never offer Restore: file and hunk actions
  unstage through audited Git tools while preserving working-tree content. Commit
  renders one validated exact commit; Branch uses a validated merge-base range,
  and both are read-only locally and over SSH. Last turn is reconstructed from
  the newest turn's recorded patches, warns when its view is necessarily partial,
  and never silently substitutes an older edited turn. Whole-diff Stage all,
  Revert all, and Unstage all actions carry only the paths visible in Review;
  Last turn Revert all uses the same atomic reverse-patch engine as `/undo`.
- Bundled skill marketplace entries now expose the requested on-demand skill packs
  without increasing the default prompt: LLM Advisor, Browser Use, OpenClaw Video
  Toolkit, and BurstyRouter appear as installable Extensions/command-palette
  actions, and command-palette search indexes their compact intent summaries so
  terms like model selection, browser automation, video/media, and local-first
  routing find the relevant pack without injecting those playbooks into the base
  prompt. Their audited install commands clone/copy the bounded `SKILL.md`
  entry point and write a matching `.quillcode/skills/*.json` manifest so
  installed skill rows replace available marketplace rows after metadata refresh.
- Structured Git sync now has first-class `host.git.fetch` and `host.git.pull`
  tools for local and SSH Remote projects, `/git fetch` and `/git pull`
  slash commands, command-palette entries, Auto safety policy coverage, prompt
  guidance, mock LLM routing, and Playwright harness support. Fetch defaults to
  `origin`; pull defaults to `--ff-only`, and explicit non-fast-forward slash
  pulls require a named remote or branch.
- Structured Git branch lifecycle now has first-class `host.git.branch.list`
  and `host.git.branch.switch` tools for local and SSH Remote projects, `/branch`
  and `/branch create|switch` slash commands, command-palette entries, Auto
  safety policy coverage, prompt guidance, mock LLM routing, tool-card subtitle
  projection, and Playwright harness support. Branch names and optional create
  start points use the shared bounded Git name validator, and start points are
  rejected unless the request is a create-and-switch operation.
- Transcript sharing now has a native keyboard/menu path: `copy-conversation`
  owns `Cmd+Shift+C`, shows that shortcut in the command palette and Keyboard
  Shortcuts panel, and routes the desktop menu action through the same Markdown
  transcript exporter used by visible Copy conversation and Export Markdown
  actions.
- Obvious branch-list, branch-switch, and branch-create prompts now route
  through deterministic immediate action preflight before a provider call, using
  the same parser as the mock LLM. Live TrustedRouter smoke covers branch-list
  and transcript integrity permits legitimate zero-argument read tools such as
  branch-list, worktree-list, and browser-inspect while still rejecting empty
  shell/file/write actions.
- Run receipts now persist model-aware token usage events, price usage from the
  live model catalog when pricing metadata is available, and render a Run
  Receipts section in Activity with per-call rows, thread total, unpriced legacy
  audit rows, and a normalized spend-fuse status from the shared
  `RunSpendLedger`. Agent runs now hard-pause after a priced model call crosses
  the configured spend-fuse bucket, render a focused Spend Review tool card with
  Continue/Stop actions, and resume only after the user approves the next spend
  bucket. Optional local day/week/month spend caps now use the same Spend Review
  approval path: the runner combines the live active thread with the workspace
  thread snapshot, pauses when a configured period cap is reached, and tracks
  thread-fuse/daily/weekly/monthly approvals as distinct buckets. Period-cap
  Spend Review cards and approval decisions now name the breached daily, weekly,
  or monthly cap so the visible transcript and audit trail match the enforced
  limit.
- Project run hooks now execute project scripts before and after each agent run
  through the normal `host.shell.run` tool-card path. Local hooks are
  discovered from `.quillcode/hooks/before-agent-run/*.sh` and
  `.quillcode/hooks/after-agent-run/*.sh`, can be extended and capped from
  `.quillcode/config.toml`, share bounded JSON sidecar metadata with local
  environment actions, abort the run on before-hook failure, and report
  after-hook failures without hiding the agent's completed answer. SSH Remote
  projects discover bounded default hook scripts during context refresh and
  route hook execution over SSH through the same tool-card boundary.
- Standard plugin `hooks/hooks.json` files and explicit manifest `hooks`
  references are discovered as bounded data and shown in the focused Hooks
  pane and `/hooks` command. Trust and disable decisions are workspace-local
  and bind to the exact normalized definition hash, so any command or metadata
  change returns the hook to Review required. Trusted synchronous command
  handlers for matcherless/`*` `UserPromptSubmit` and `Stop` events reuse the
  visible before/after `host.shell.run` pipeline. Matching handlers launch
  concurrently and receive the standard lifecycle JSON on stdin. Package root
  and private workspace/plugin-scoped data paths are exposed through both
  `PLUGIN_*` and `CLAUDE_PLUGIN_*` compatibility variables, while stdin and
  environment values are redacted from durable tool events. User-prompt stdout
  and structured additional context enter the model as bounded hidden system
  context; warnings remain visible notices; documented block/continue decisions
  and exit code 2 stop prompts or request one loop-bounded Stop continuation.
  `continue:false` wins over concurrent continuation requests, and the continued
  Stop payload reports `stop_hook_active=true`. Trusted `PreToolUse` and
  `PostToolUse` command handlers match bounded regular expressions against
  canonical `Bash`, `apply_patch`/`Edit`/`Write`, and `mcp__server__tool` names.
  Pre hooks may deny, add hidden context, or rewrite only the current tool's
  input before safety review; post hooks receive the actual success or failure
  and may add context or replace model-facing feedback without undoing effects.
  Rewritten calls survive approval pause/resume without rerunning pre hooks.
  Trusted `PermissionRequest` hooks run only for approvable gates, receive the
  effective canonical call plus a bounded approval description, and aggregate
  deny-wins/allow/abstain decisions without overriding hard safety denials.
  Failures preserve the normal durable approval UI; reserved input, permission,
  and interrupt mutations are rejected.
  Trusted `PreCompact`/`PostCompact` hooks bracket manual and automatic
  compaction with `manual`/`auto` trigger matchers. Pre stops happen before
  summary or state mutation; Post runs only after a real compaction and may stop
  an automatic model retry without discarding the compacted thread. Failed,
  timed-out, or malformed hooks warn visibly and preserve compaction progress.
  Trusted `SessionStart` hooks match `startup`/`resume`/`clear`/`compact`, run
  once per corresponding live thread lifecycle, and add bounded hidden context
  before the model turn. Trusted `SubagentStart`/`SubagentStop` hooks match the
  worker type and receive parent session/turn identity plus worker metadata.
  Stop hooks may request one durable worker continuation; an explicit stop
  wins, and a second continuation is ignored to prevent loops.
  Remaining standard events, prompt/agent handlers, and asynchronous handlers
  are parsed and labeled Unsupported rather than silently executed.

### Packaged interaction evidence

- Settings parity requires identified dialog content and a real Close action. The packaged macOS gate AX-presses Settings, requires the title and first preference control, dismisses through the identified Close control, and rejects shallow state-only evidence.
- Automations parity requires identified pane content and an obvious exit. The packaged macOS gate AX-presses Automations, requires the title and Create control, dismisses through the identified Close control, and rejects shallow state-only evidence.
- Extensions parity requires identified pane content and an obvious exit. The packaged macOS gate AX-presses Extensions, requires the title and Add control, dismisses through the identified Close control, and rejects shallow state-only evidence.
- Review parity requires discovery without another permanent sidebar row, visible content even when a long transcript was following its tail, and an obvious exit. The compact Tools menu exposes Review through the same `toggle-review-panel` command used by shortcuts, native menus, HTML, and the pane Close action. A tri-state presentation policy preserves automatic diff presentation while allowing an explicitly opened empty or failed review to show scope controls and diagnostics; the next toggle closes it predictably. Opening Review scrolls its stable anchor into view; closing it restores transcript-tail behavior. The packaged macOS gate normalizes Review to hidden, AX-presses the real command, requires the title and scope control, dismisses through the identified Close control, and rejects state-only evidence.
