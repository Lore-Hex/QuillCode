# Roadmap

## Milestone 0: Compile-Stable Foundation

- The public `quill-code mcp-server` bridge exposes the exact Codex `codex` and `codex-reply`
  tool names over strict JSON-RPC stdio, runs the real QuillCode agent, persists durable replies,
  projects bounded progress, handles approvals and cancellation, and cleans up on EOF. Exact native
  event/patch metadata and OS-sandbox `on-failure` escalation remain tracked parity work rather than
  being represented as complete.
- SwiftPM package, docs, CI skeleton.
- Core models, mock agent, shell/file/git tools.
- Auto safety policy and reviewer protocol.
- Persistence and secret-store API.
- Unit tests for direct command execution and safety behavior.
- Current status: shell, file read/list/write/search, apply patch, git status/diff, git fetch/pull, git branch list/switch/create, file/hunk-level git stage/restore, local git commit, named-remote git push, GitHub PR list, PR creation, PR view/comments, PR checks, PR diff, PR checkout, PR comments, PR reviews, PR reviewer requests/removals, PR label add/removal, PR close/reopen, PR merge/automerge, and git worktree list/create/open/remove/prune are implemented with unit coverage. CI runs on pushes, pull requests, and merge-group candidates, and a serialized merge-train workflow is available for agent PRs labeled `merge-train`.
- Non-interactive automation now has a dedicated `QuillCodeCLI` module and `quill-code exec` contract: live/mock selection, read-only/workspace-write/danger-full-access modes, Git guarding, bounded stdin, JSONL lifecycle events, ephemeral runs, exact/last resume, atomic final output, bounded JSON Schema validation, signal-aware cancellation, and global/project MCP startup with native tool exposure. Danger full access explicitly removes the built-in workspace path boundary for shell working directories and file tools in both exec and app-server while retaining review policy, edit guards, output bounds, and secret protections. Required MCP failures stop before model invocation, thread/turn start events, attachments, or persistence while retaining failure events; optional failures are isolated; all opened sessions terminate across success, failure, construction errors, partial startup, and SIGINT. SIGINT cooperatively cancels active model/tool work, persists the stopped partial task, exits 1 without a false successful terminal event, and skips final-output/schema handling. `quill-code app-server` now implements the Codex-compatible stdio core: strict initialization, durable thread lifecycle and goals, streamed turn/item events, steering, interruption, managed local images, command/file approval round trips, model/provider discovery, non-secret account state plus API-key/browser OAuth login, cancellation, logout, and ordered account notifications, local token/spend summaries, structured config reads plus atomic value/batch writes with content-version conflicts, local marketplace `plugin/list`, installed/suggested `plugin/installed`, and local bundle-detail `plugin/read`, shared Open Agent Skills listing, persistent skill enablement, bounded extra roots and watched invalidation, binary-safe host filesystem read/write/metadata/directory/copy/remove, connection-scoped filesystem watches, notification opt-outs, message bounds, EOF cleanup, and MCP status/reload/OAuth/tool/resource methods over shared stdio or HTTP sessions with global and thread-project config. Plugin detail discovery is data-only and projects product-compatible skills, exact hook keys, app metadata, and MCP names while rejecting symlink/path/size escapes. MCP status preserves exact wire-shaped inventory and offers a tools/auth-only path that skips heavier resource enumeration; OAuth returns a real authorization URL, persists refreshable credentials, reports asynchronous completion, and reloads the registry. App-server and exec turns share one MCP schema/routing adapter; ready tools use schema-preserving `mcp__server__tool` definitions and exact raw-name execution routes. Required servers fail thread start/resume/fork before persistence, and reload refreshes the next turn's inventory. Focused unit/integration tests plus real executable exec/app-server smoke cover the contracts. Provider-owned quotas, remote plugin catalogs and mutation methods, remote-only `plugin/skill/read`, and websocket transport remain later CLI milestones.
- App-server manual compaction now mirrors Codex's response-first
  `thread/compact/start` contract. It uses the shared live/mock compaction policy,
  emits one non-steerable `contextCompaction` turn, persists progress transactionally,
  runs the normal compact hooks, and remains interruptible without mutating history
  after cancellation.
- App-server `thread/rollback` now mirrors Codex's history-only contract. Stable persisted turn and
  client-message identities keep steering grouped across relaunch, explicit names remain distinct
  from generated desktop titles, active turns/compactions exclude rollback, and atomic persistence
  never pretends to reverse workspace or account side effects.
- `quill-code doctor` now provides the Codex-compatible support surface across installation, runtime,
  config, auth, Git, terminal, MCP, sandbox, state/task inventory, TrustedRouter reachability, and
  app-server checks. Collection is bounded and read-only, JSON is stable and redacted, and focused plus
  executable tests prove corruption handling, non-disclosure, authorization, and zero state mutation.
- `quill-code review` now covers the stable Codex target forms for uncommitted work, a base branch,
  one commit with an optional title, and custom argument/stdin criteria. CLI and native review share one
  typed engine and report sink; CLI review is ephemeral and capability-filtered to bounded read/search
  and Git inspection. Focused real-repository tests plus an aggregate process smoke prove target
  scoping, cancellation, fail-closed completion, large-diff handling, and zero task persistence.
- Named local worktree tasks now have a one-click **Publish branch** path. QuillCode inspects the real branch, base, upstream, dirty state, divergence, and existing PR before using the standard visible push/PR tool cards; dirty and diverged states stop without remote mutation.
- Published worktree tasks persist exact PR identity and expose explicit Land, Refresh, and post-merge Clean up actions. QuillCode resumes queued-status reconciliation after relaunch or task selection, polls off the main actor, and repairs already-missing merged bindings without ever deleting an existing checkout in the background.

## Milestone 1: Native Workspace UI

- QuillUI shell with sidebar, top bar, composer, transcript, and tool cards.
- Project picker and model picker.
- Settings with OAuth/dev override and mode selection.
- Reviewable **Import from another agent** migration for Claude Code projects, recent chats, instructions, settings, skills, plugins, MCP servers, hooks, slash commands, and subagents, with additive writes, secret redaction, deduplication receipts, and explicit trust/auth follow-ups.
- Native smoke tests.
- Current status: testable workspace state model, persisted config/thread/project bootstrap, bounded broad-to-specific project instruction loading from root and nested `.quillcode/rules`, bounded memory loading from `~/.quillcode/memories` and project `.quillcode/memories`, explicit `/remember text` global memory writes, agent-callable `host.memory.remember` writes for user-requested durable preferences/facts, Memories-pane Forget actions, and redacted Memories-pane review rows for blocked credential-like memory attempts, bounded project-local extension manifest discovery for plugins, skills, and MCP servers, explicit MCP stdio server start/stop lifecycle controls with bounded `initialize`/`tools/list` readiness probes, generic allowlisted MCP `tools/call` routing for Ready servers, HTML surface contract, Playwright harness, smoke script, packaged macOS app smoke, and SwiftUI desktop shell exist for project rail, visible New chat/Search/Open project/Browser/Terminal/Memories/Extensions/Activity sidebar actions, desktop folder picking, typed Local/SSH Remote project metadata, `Project: Add SSH Remote...` and `/ssh user@host:/path` registration, project row actions for New chat/Refresh context/Rename/Remove from list with SSH Remote context refresh over SSH, SSH Remote git status/diff/stage/restore/commit/push/PR/worktree execution, bounded SSH Remote file read/list/write, SSH Remote apply-patch and review actions with remote diff refresh, thread rename/duplicate/pin/archive/unarchive/delete controls, bulk chat select/select-all/pin/unpin/archive/unarchive/delete controls, pinned/recent/archived chat grouping, stable All/Pinned/Recent/Archived saved sidebar filters with counts and visible-row bulk selection, thread search that excludes hidden tool-feedback state and supports keyboard result highlighting plus Enter selection, active-chat `Cmd+F` transcript Find, grouped/ranked command palette with keyboard navigation, registry-backed primary keyboard shortcuts, command-derived `Cmd+/` Keyboard Shortcuts discoverability, local environment action commands from `.quillcode/actions`, `.quillcode/local-env`, and optional bounded extra directories in project `.quillcode/config.toml` with optional display/environment/working-directory/timeout metadata sidecars plus workspace-bounded shell cwd, structured environment overrides, redacted environment values in transcript/tool-card payloads, and timeout routing, browser preview state/comments/snapshots with bounded HTTP(S) HTML fetch upgrades, explicit inspection-depth labels, and structured browser open/inspect tools, multiline composer parity in SwiftUI/static HTML/Playwright with shared-catalog keyboard-navigable slash suggestions, Shift+Enter newlines, Enter-to-send, active-run Stop, structured `/pr create|view|checks|diff|checkout|comment|review|reviewers|labels|close|reopen|merge` commands routed through the same local/SSH Remote GitHub PR tools as agent calls, and structured `/worktree create|open|remove|prune` commands routed through the same local/SSH Remote worktree flow as visible UI, clustered in-app top bar with bounded context/status labels, instruction/memory/Computer Use status, and native macOS menu-bar widget, derived right-side Activity pane for deterministic or model-authored task-plan/current-task/source/tool/artifact/latest-answer/handoff state with command-driven collapsible sections, chronological event-driven transcript, transcript copy actions with visible copied feedback, user-message Use as draft, no assistant rating controls, latest-assistant Retry action over the shared retry command, estimated context pressure banner with Compact context/New thread/Fork from last actions, deterministic or model-backed compact/fork continuation threads with replayable `model` vs `deterministic_fallback` telemetry, incremental agent-run progress, bounded multi-step agent tool continuation, tool-card presentation with human final-answer bubbles, stopped-tool cleanup, artifact preview chips plus bounded image previews with shared type/source metadata, and collapsed raw details for successful tools, integrated workspace terminal command history with Up/Down recall and live stdout/stderr streaming, persistent per-project cwd and environment deltas, running/done/failed/stopped lifecycle, and Stop controls, git diff review summaries with post-patch refresh, file/hunk Stage/Restore controls, and file-scoped review notes, git worktree command-palette actions, safe dry-run prune action, review/confirm stale-worktree prune dialog, create/open/remove dialogs, open/remove dialogs with known-worktree pickers, local and SSH Remote picker loading/error/retry states without transcript tool-card noise, and create/open-time local/SSH Remote worktree project/thread handoff, slash commands for core workspace actions, project management, memory visibility/writing/forgetting, context compaction, thread lifecycle, and local environment scripts, searchable grouped model selection with keyboard result highlighting and Enter selection, persistent favorites, recent-model surfacing, current/default/recommended/favorite badges, deterministic model metadata rows, inline model detail browsing, mode switching, and native developer settings. The desktop app seeds the current working directory as the initial project and runs tools from the selected local project path.
- Concurrent chats now own independent agent tasks instead of sharing one global send slot. Users can start work in another chat while the first continues in the background; each run keeps its original project/worktree, draft, progress, retry notices, completion notification, and sidebar status, while Stop All cancels all active chat runs.
- Trusted standard plugin `UserPromptSubmit` and `Stop` command hooks now receive the documented newline-terminated JSON lifecycle payload, package root and private persistent plugin-data compatibility variables, concurrent matching-handler launch, deterministic transcript ordering, and complete stdin/environment redaction. Their bounded stdout/JSON contract now supplies hidden user-prompt context, visible warnings, prompt blocking, `continue:false` precedence, semantic exit-code-2 handling, and one durable loop-bounded Stop continuation with the correct active flag.
- Trusted standard plugin `PreToolUse`, `PostToolUse`, and `PermissionRequest` command hooks now cover shell, patch, and MCP agent calls. Regex matchers use Codex canonical names and patch aliases; all matching commands launch concurrently and aggregate deterministically. Pre hooks can deny, add hidden context, or validate and rewrite only the current input before safety review, including durable approval resumes without duplicate pre execution. Post hooks receive actual success/failure payloads and can add context or replace model-facing feedback without claiming side effects were undone. Permission hooks resolve only approvable gates with deny-wins aggregation; hard safety denials remain absolute, and failed or silent hooks preserve normal approval.
- Trusted standard plugin `PreCompact` and `PostCompact` command hooks now share one typed lifecycle across manual, proactive, and reactive compaction. Trigger matchers distinguish `manual` from `auto`; pre hooks run before summary/state mutation; post hooks run only after a real compaction; explicit stops preserve the correct before/after durability boundary; and hook process/parser failures warn without sacrificing compaction recovery.
- Trusted standard plugin `SessionStart`, `SubagentStart`, and `SubagentStop` command hooks now run at explicit primary-thread and delegated-worker boundaries. Session sources distinguish startup, resume, clear, and compact without parsing transcript text; start context enters hidden model context; parent/worker identity and nullable child transcript paths follow the standard payload; and SubagentStop may request exactly one durable continuation while `continue:false` wins. Matching commands remain concurrent, deterministically folded, bounded, and failure-tolerant.
- Codex-style `/side` and `/btw` now create a transient history fork without interrupting the parent task. Side conversations inherit project, model, instructions, memories, tools, and the visible transcript for reference; stay out of persistence and sidebar projections; display the parent run status and an explicit Return action; prohibit nested side conversations and subagents; and discard all side-only state on Return or navigation. Swift lifecycle/prompt/persistence/concurrency tests and Playwright UI coverage verify that the parent transcript and draft remain untouched.
- Runnable local-environment tasks now appear in a contextual top-bar **Actions** group for quick access, while the permanent header remains limited to the existing quiet Codex-style controls. Native, HTML, and Playwright surfaces dispatch the same command IDs used by the command palette and `/env`.
- Compact context and fork-summary now show an in-banner running state while the summary is being prepared, and temporarily disable competing context-move actions so those buttons do not feel like dead clicks.
- Codex-style `/clear` now clears the current chat transcript, event/tool history, queued follow-ups, and draft without deleting the thread shell, project, mode, model, instructions, memories, or pin/archive state.
- Codex-style `/undo` now reverses the latest currently revertable local turn's file edits through the same honest reverse-patch engine as the transcript Revert button, and is available from slash commands, the command palette, and the HTML/Playwright harness.
- Codex-style `/pin` and `/unpin` now update the selected chat's pinned state through the same persisted lifecycle path as sidebar row actions, with idempotent command behavior and native/HTML harness parity.
- Codex-style `/delete` now removes the selected chat through the same persisted thread-delete command path as the command palette and sidebar row action, with native/HTML harness parity and copy that distinguishes chat deletion from project file deletion.
- Codex-style `/fork [last|summary|full]` now routes through the existing context continuation thread strategies, with alias support for `/fork-last`, `/fork-summary`, and `/fork-full-context` plus native/HTML harness parity.
- Durable `/goal` slash commands now attach an objective to the selected chat, support status/complete/block/resume/clear, persist with the thread, project active/blocked/completed state into the top bar, feed the active goal into the TrustedRouter prompt, and have Swift plus Playwright lifecycle coverage.
- The Git review pane now has explicit Unstaged, Staged, Commit, Branch, and Last turn scopes in native SwiftUI and the browser harness. Commit renders the patch introduced by one validated commit reference; Branch compares `HEAD` with a validated base branch through Git's merge base. Historical comparisons are read-only, while Unstaged/Staged action refreshes preserve their typed scope and successful empty results remain visible with honest copy. Last turn is derived from the newest turn's real recorded `apply_patch` provenance, coalesces repeated file edits, stays read-only over SSH, and warns when non-patch mutations make the view partial. Stage all, Revert all, and Unstage all act on the exact visible file set through shared validated local/SSH Git paths; Last turn Revert all uses the existing atomic reverse-patch engine. Local and SSH Remote projects share the same command semantics and action planner, including cached reverse-patch hunk unstage that preserves working-tree edits.
- Structured Git sync now exposes `host.git.fetch` and `host.git.pull` for local and SSH Remote projects, plus `/git fetch` and `/git pull` slash commands, command-palette entries, prompt guidance, mock LLM routing, Auto safety coverage, and Playwright harness behavior. Fetch defaults to `origin`; pull defaults to `--ff-only`, and explicit merge-style slash pulls require a named remote or branch.
- Structured Git branch lifecycle now exposes `host.git.branch.list` and `host.git.branch.switch` for local and SSH Remote projects, plus `/branch`, `/branches`, `/branch switch name`, and `/branch create name --from ref` slash commands, command-palette entries, prompt guidance, mock LLM routing, Auto safety coverage, tool-card subtitles, and Playwright harness behavior. Branch names and optional start points share the bounded Git name validator, and start points are accepted only for create-and-switch requests.
- SSH Remote chat execution now uses the same noninteractive SSH path as the integrated terminal for `host.shell.run`, bounded `host.file.read`/`host.file.list`/`host.file.write`, `host.apply_patch`, `host.git.status`/`host.git.diff`, file/hunk `host.git.stage`/`host.git.restore`, `host.git.commit`, `host.git.push`, `host.git.pr.list`, `host.git.pr.create`, `host.git.pr.view`, `host.git.pr.checks`, `host.git.pr.diff`, `host.git.pr.checkout`, `host.git.pr.comment`, `host.git.pr.lifecycle`, `host.git.pr.review`, `host.git.pr.reviewers`, `host.git.pr.labels`, `host.git.pr.merge`, and `host.git.worktree.*`, while environment tools are filtered out for remote projects and guarded at execution time if a model still asks for one. Explicit SSH Remote Refresh context reads bounded remote `AGENTS.md`, `.quillcode/rules.md`, `.quillcode/instructions.md`, and project `.quillcode/memories` files into the selected thread context. Remote shell/file/patch/git tool cards and terminal entries also carry explicit execution-context chips/rails so transcript history remains clear after project switches. SSH Remote terminal commands now preserve cwd and exported/unset environment deltas across commands through stripped marker metadata.

## Milestone 2: TrustedRouter Runtime

- OAuth PKCE and delegated key storage.
- Streaming chat client.
- Live model catalog native UI.
- Auto reviewer using `glm-5.2` with `kimi-k2.6` fallback in the native app.
- Current status: live TrustedRouter adapter exists behind CLI `--live`; desktop env/secret-key runtime selection, OAuth-first auth mode state, native loopback PKCE sign-in, pure authorize/exchange/userinfo OAuth client, automatic delegated key storage, non-secret signed-in account display, secondary developer key/base-URL settings, live model-catalog refresh with flexible capability metadata decoding, startup and periodic keyed catalog refresh for stale provider-health metadata, visible bundled/live/fallback freshness labels, catalog-derived provider-health summaries in the model picker and Settings, CLI key-management commands, incremental agent progress callbacks, bounded multi-step tool continuation, streamed action-text transport, canonical action parsing with near-miss schema alias normalization, visible Streaming status, safe streamed assistant drafts for `say` actions, shared actionable runtime issue UI for sign-in/key/rate-limit/network/empty-response/malformed-action failures, Retry-last-turn recovery from transient runtime failures, malformed-response and rate-limit recovery through the searchable model picker, cheap catalog-selected auxiliary models for context summary and compaction calls, and redacted runtime diagnostics with parsed rate-limit metadata in Settings are implemented. Account recovery telemetry and first-party provider status beyond catalog metadata remain the next runtime polish.

## Milestone 3: Codex Workflow Parity

- Full PTY terminal sessions with job control and TUI handling, richer worktree branch lifecycle polish, local env orchestration, and richer GitHub review workflows. A pseudo-terminal foundation now exists: `PTYProcessSession` runs a command attached to a `posix_openpt` master/slave pair so programs that probe `isatty()` get true terminal behavior, streaming merged terminal output through the existing `ShellProcessEvent` contract with the same cancel/timeout/exit-code semantics as the pipe runner, propagating the terminal window size (rows/columns) to the child through `TIOCSWINSZ` so `stty size`/ncurses TUIs lay out correctly, accepting interactive keystroke input through `sendInput` so shells, REPLs, and prompts can be driven, and re-applying the window size to a running session through `resize` so `SIGWINCH`-aware programs reflow when the terminal changes. The live local workspace terminal now launches commands through that PTY path, seeds new sessions from the SwiftUI terminal pane's measured cell size, forwards pane-size changes to the active local PTY session, exposes Stop/Suspend/Resume, and renders common ANSI/cursor-addressed TUI redraws through a single-page screen buffer while SSH Remote terminal commands stay on the noninteractive SSH pipe path. The renderer handles bounded scroll regions, reverse-index, explicit scroll up/down, insert/delete line, alternate-screen latest-frame preservation, width-two CJK/emoji/emoji-presentation advancement, ZWJ grapheme-cluster preservation, combining-mark cursor accounting, narrow-by-default ambiguous-width policy for stable transcript scrollback, automatic wide ambiguous-width policy for CJK locale terminal surfaces, a `QUILLCODE_TERMINAL_AMBIGUOUS_WIDTH` override for support/debugging, and native/HTML SGR rendering for standard, bright, 256-color, and RGB terminal output with common emphasis attributes. DEC `1000`/`1002`/`1003` mouse negotiation with X10/UTF-8/SGR/URXVT encoding is parsed from terminal output; native left/middle/right click, drag, any-motion hover, and quantized trackpad/mouse-wheel input map to terminal cells and are written back to the active PTY with stale-mode and lifecycle guards. A focused native adapter now also delivers printable, control, Option, navigation, editing, function-key, and paste input, honoring DEC application-cursor and bracketed-paste modes with bounded paste payloads and stale-mode guards. Full curses terminal-emulator semantics, richer process-group job control, and deeper alternate-screen history remain the next steps.
- Managed local Worktree tasks now start detached from the selected/current branch and transactionally carry the user's current work into the isolated checkout. Staged and unstaged tracked patches retain their index state; bounded untracked files, `.worktreeinclude`-selected ignored files, and ignored `AGENTS.override.md` are copied without following symlinks or overwriting base content. Any apply/copy failure force-removes the newly registered worktree so a failed task cannot leave a half-materialized checkout. Detached managed tasks can hand off both uncommitted task state and committed history between Local and their stable associated Worktree. Committed history moves only by a verified fast-forward into a clean destination; backward or diverged moves fail without intentional mutation, and a failed later transfer step restores the prior destination commit. A detached Worktree task can also use **Create branch here** to claim a validated Git branch without moving the thread or checkout; QuillCode persists the discovered branch owner and removes Create branch/Handoff once ownership exists. **Finish task in Local** composes the verified Handoff with non-force Git removal, clears the durable binding only after cleanup succeeds, and leaves a Local, retryable **Finish worktree cleanup** state if concurrent changes prevent removal. Managed tasks live under `~/.quillcode/worktrees` by default or a user-selected absolute root, and each binding persists the root that authorized it. Archiving or recent-task cleanup saves a private, repository-bound snapshot of staged, unstaged, and bounded local-file state before removing a registered disposable worktree. Cleanup defaults to the 15 most recent tasks, can be disabled or resized in Settings, and re-enforces after creation, startup, unpinning, run completion, or leaving a selected task. Reopened tasks show a saved state and one-click restore at the exact captured commit; corrupt payloads and partial apply failures cannot leave a half-restored checkout. Pinned, selected, running, Local, and named-branch tasks are retained. Top-bar, sidebar, command-palette, native, HTML, and Playwright surfaces expose these state-specific actions. New Worktree Task now offers Automatic, No setup, and bounded project-defined named environments before creation; the exact selection persists on the task binding. The materialized checkout resolves the selected platform-specific or fallback script and runs it visibly through `host.shell.run` before returning control; setup output, failure, timeout, and rerun remain in the transcript while failed checkouts stay available for diagnosis.
- Per-file project-instruction scopes now attach to loaded `AGENTS.md`, `.quillcode/rules.md`, and `.quillcode/instructions.md` records, flow into TrustedRouter hidden system context, and show in Activity sources. Activity also surfaces duplicate-scope, nested-overlap, explicit nested-override, and conservative semantic-conflict diagnostics for explicit opposing instructions over overlapping scopes, then promotes conflicts into a dedicated Instruction Review section with issue counts and toggle controls. Source rows expose typed Open/Edit actions, conflict diagnostics expose line-specific Open Source/Edit Source plus Resolve/Dismiss actions, Resolve drafts include precise `file:line` source targets with excerpts and suggested edits, dismissed diagnostics persist as project-scoped resolution records, and diagnostics that disappear after metadata refresh are recorded as resolved-by-edit audit records. Local desktop source actions open readable workspace files in the native editor at the exact line when possible and fall back to the model file-read/edit-draft path otherwise. Deterministic two-line semantic conflicts now expose direct keep-side patch actions that remove the opposite conflicting line through `host.apply_patch`; exact duplicate same-scope diagnostics expose direct Clear duplicate actions through `host.file.write` with explicit empty content; repeated broad lines inside nested instruction files expose direct Remove repeated lines actions through `host.apply_patch`; explicit nested override diagnostics expose direct Remove override lines actions through `host.apply_patch` when loaded excerpts still match. These action types refresh project context after success; non-identical duplicate merges and semantic nested override rewrites remain pending.
- Explicit subagent workflows now have a replayable `host.subagents.update` progress schema, a visible Activity Subagents section, a deterministic `/subagents objective | Name: role` slash command, and a parallel scheduler that emits queued/running/completed/cancelled progress through the same event/tool contract. Normal model turns can also call structured `host.subagents.run`; that call launches the same real scheduler, publishes live worker rows, persists the parent manifest while work is running, and returns bounded worker summaries to the parent model for one-turn consolidation instead of merely decorating Activity. Stop All preserves stopped state and cancelled progress. Each production worker runs an isolated multi-step `AgentRunner` session built by the normal configured send-session factory, inheriting project, worktree, model, mode, instructions, memories, goal, permission rules, MCP/browser/Computer Use tools, and local or SSH Remote routing. Worker summaries and compact inline Activity transcripts are bounded and redacted, safety blocks are reported honestly, and progress stays pinned to the originating chat across selection changes. Direct child sessions cannot open an independent `host.subagents.run` tree; bounded recursive delegation remains scheduler-owned through `[[DELEGATE: name | role]]`. The scheduler supports dependency waves, prerequisite failure propagation, cycle termination, completed-summary forwarding, bounded `xN` concurrency, hierarchical paths, and bounded recursive spawning. Scheduler-owned child transcripts remain private and durable, compact parent manifests survive relaunch, interrupted work is never replayed implicitly, Activity retains historical full-transcript drilldowns, and exact generation-bound Run/Skip actions consume owner-only payloads once and resume only the affected graph. Older whole-session journals remain migration-readable. Richer recursive-subagent controls and autonomous background scheduling remain pending.
- Browser rendering adapter, live DOM/page inspection implementation for dynamic pages, richer browser comments, and browser-open snapshot fetching from agent-driven navigation. The live DOM capture contract now exists behind `BrowserLiveDOMCapturing`, and the macOS desktop app can upgrade fetched HTTP(S) pages through an offscreen WebKit rendered-DOM capture that reuses WebKit's persistent cookie/session data store by default. A reusable visible macOS WebKit browser-session window can now be opened from the browser pane, menu bar, command palette, or `/session [target]` slash command to sign into sites using the same profile. Browser preview state now has focused tab records with isolated history/snapshot/comment state plus native/static HTML/Playwright tab create, switch, and close flows. The macOS visible WebKit session now consumes a shared browser-session sync snapshot, renders the current browser tab set as native tabs, reports visible-session tab selection/title/URL changes back into the preview model through an app-layer update contract, and sends rendered live-DOM snapshots from completed visible-session navigations through the same app-layer snapshot model as offscreen captures. App-driven Browser Back/Forward/Reload now also call through to the visible WebKit tab: Back/Forward prefer WebKit history with model-sync fallback, while Reload covers the unchanged-URL refresh case that snapshot sync intentionally skips. The visible-session adapter can evaluate JavaScript in the selected WebKit tab, return a bounded textual result, explicitly capture a selected-tab live DOM snapshot on demand, and drive selector-safe selected-tab click/type actions through focused adapter methods; desktop `host.browser.inspect` now prefers that visible live DOM capture before falling back to the model snapshot. Model-routed `host.browser.click`, `host.browser.type`, and `host.browser.script` now execute against the open visible WebKit session and return structured action/script summaries for final-answer rendering. Linux desktop builds now have a headless Chromium/Chrome browser-process live-DOM adapter that renders HTTP(S)/file URLs with isolated profile state and bounded dumped DOM capture when an installed browser is available. Native desktop smoke now opens local HTML, preserves a browser comment, and proves the composer-driven `host.browser.inspect` path in release artifacts. Linux visible-session control still needs implementation.
- Computer Use platform backends, app approvals, and agent wiring. macOS now has permission-aware status, screenshot/input primitives, Settings onboarding actions, focused-app detection, and structured agent tools for screenshot/click/type/scroll/move/key dispatch through the active backend; Linux now has helper-backed screenshot/input routing for Wayland and X11 when required helpers are installed. Agent dispatch honors optional config-backed app approvals by foreground bundle identifier or app name, and Settings exposes a focused approval-management card that persists bundle IDs/app names through the same settings save path. Settings can quick-add the detected foreground app to the draft allowlist by bundle identifier when available, falling back to app name when needed. Screenshot tool output carries the detected foreground app, dimensions, preview artifact path, a bounded macOS accessibility snapshot when Accessibility is granted, and a bounded visual-summary descriptor as structured metadata. The same private per-thread screenshot artifact now continues to image-capable TrustedRouter models as hidden multimodal feedback, allowing the next tool decision to inspect pixels without adding raw base64 to transcript state or duplicating the image. Richer onboarding, packaged Accessibility interaction smoke, and advanced recovery remain.
- Record & Replay parity is implemented on macOS through the shared Computer Use boundary. Extensions, the command palette, and the menu-bar widget expose a focused Record/Stop lifecycle; start requires one non-bypassable explicit confirmation that discloses TrustedRouter processing, while stop remains immediate after permission changes. The recorder bounds and privately stores app/action/screenshot capture, redacts protected text fields, rejects queued events after stop, generation-binds monitor startup, surfaces its 30-minute limit, samples screenshots from first through final, cancels under Stop All/Disconnect All, and preserves the originating task across selection changes. Stopped capture continues through the ordinary TrustedRouter agent and audited file tools to create one reusable `.quillcode/skills/<slug>/SKILL.md` with purpose, variable inputs, generalized steps, and verification. Native lifecycle/safety/integration tests plus Playwright mock-LLM interaction coverage verify the complete flow.
- Full embedded PDF/doc/sheet/slide/appshot artifact renderers, signed remote marketplace distribution, MCP streaming beyond the generic verified-server call path, and richer prompt/resource UX. Structured PDF/document/spreadsheet/presentation/appshot artifact cards now exist in the shared tool-card surface, including `.appshot.json` bundle classification without raw JSON text-preview noise. Project extension manifests can expose explicit bounded install/update commands that run through the same shell tool-card path and refresh project metadata afterward. Project-local `.quillcode/marketplace/*.json` entries provide the legacy bounded marketplace lane. Standard repo `.agents/plugins/marketplace.json` and legacy `.claude-plugin/marketplace.json` catalogs now expose local `./` Codex packages through the same Plugins pane and install them with a typed, staged, atomic, lifecycle-script-free copy after execution-time identity, boundary, symlink, file-count, byte-count, and destination checks. Standard installed packages project bounded bundled skills and `.mcp.json` servers into the live Extensions/agent/MCP runtime without executing discovery-time code. Standard package hooks are now inspectable with exact-definition trust, and trusted synchronous command handlers cover prompt, stop, tool, permission, compaction, session, and subagent lifecycle boundaries; signed git/npm acquisition, personal marketplace support, app/connectors, additional event families, prompt/agent handlers, asynchronous hooks, and streaming MCP results remain follow-ups. MCP resource reads, prompt gets, and bounded per-tool input schema summaries run through allowlisted Ready-server agent tools and the Extensions UI. Ready MCP resources and prompts are directly actionable from the Extensions pane and command palette, with results recorded as normal tool cards and assistant summaries. Explicit memory editing and Forget actions now cover global, local project, and SSH Remote project memory files, and the Memories pane now surfaces deterministic review rows for blocked credential-like memory attempts plus obvious opposing global/project memory notes with edit actions. Deterministic event-source adapters now exist through the `AutomationEventSource` protocol: `FileChangeEventSource` fires when a watched file appears or is modified after the last check, `DirectoryChangeEventSource` does the same for watched directory metadata, `URLLastModifiedEventSource` fires when an HTTP(S) resource reports a Last-Modified timestamp newer than the monitor's last run, and `URLFeedUpdateEventSource` fires when a bounded RSS/Atom feed reports a published or updated timestamp newer than the last run. `/monitor file|directory|last-modified|feed ...` plus the command palette/create menu can create persisted file, directory, URL header, and feed monitors without hand-editing automation JSON. The desktop automation tick resolves configured event sources for active event monitors and creates monitor threads with trigger context. Linux notification command planning now maps agent-run and automation notifications to bounded `notify-send` argv without shell interpolation, `LinuxNotificationCommandRunner` delivers that argv through a fake-helper-tested process boundary, the desktop notifier factory selects that Linux path at runtime, and persisted Settings controls now gate agent-run alerts, inactive-only run alerts, and automation alerts without restarting the desktop app. Deeper memory redaction/Chronicle jobs, fully autonomous memory inference, broader project-memory review UX, broader event-source adapters and their engine wiring, richer recursive-subagent UX controls, and deeper delegated-agent observability remain pending.

## Latest Quality Pass

- Bundled Extensions now expose curated on-demand skill installs for LLM
  Advisor, Browser Use, OpenClaw Video Toolkit, and BurstyRouter. Each install
  writes both `.quillcode/skills/<name>/SKILL.md` content and a matching
  `.quillcode/skills/<name>.json` manifest, keeping the base prompt small while
  making common Codex-style skill workflows discoverable.
- Run-receipt cost ledger enforcement is now in place: TrustedRouter usage
  records carry the model ID, Activity renders priced per-call receipts and a
  thread-total spend row from catalog pricing, legacy usage events remain
  auditable as unpriced rows, and `AppConfig.runSpendFuseUSD` is normalized with
  backward-compatible defaults. The displayed fuse crossing is backed by the
  same core `RunSpendLedger` that powers runtime enforcement, and runs now
  hard-pause on a Spend Review card until the user chooses Continue for the
  current spend bucket. Period caps share that path and surface daily/weekly/
  monthly-specific Spend Review copy and audit decisions. The top-bar budget
  meter also derives local Today/Week/Month spend rows from persisted priced run
  receipts across threads; true TrustedRouter account-history and provider
  quota rows remain future runtime work.
